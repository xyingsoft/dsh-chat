# Wiki 构建产物（由 scripts/build-wiki.ps1 生成）
/build/

# 依赖与构建
node_modules/
dist/
*.tsbuildinfo

# Yarn 4：yarn.lock 必须提交；以下为本地状态与缓存
.yarn/*
!.yarn/patches/
!.yarn/releases/
.pnp.*

# 环境与本地配置
.env
.env.local
*.local

# 编辑器与系统
.vscode/
.idea/
.DS_Store
Thumbs.db

# 日志
*.log
npm-debug.log*
pnpm-debug.log*

# 测试与手动运行留下的本地库。曾经被误提交（#31）——
# 里面只有 schema_migrations，没有个人数据，但它每跑一次测试就变一次，
# 把工作区弄脏，也让「有没有改动」这个信号失去意义
dsh-chat.db
dsh-chat.db-journal
dsh-chat.db-wal
dsh-chat.db-shm
dsh-chat.credentials.json
