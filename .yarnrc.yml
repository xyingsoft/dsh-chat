# 使用传统 node_modules 布局，与 DSH 生态的插件项目保持一致
nodeLinker: node-modules

# 不覆盖 enableImmutableInstalls：Yarn 在检测到 CI 时默认启用 immutable，
# 这层保护是白拿的，关掉它只会让某个漏加 --immutable 的 job 悄悄改写 yarn.lock。

compressionLevel: mixed
