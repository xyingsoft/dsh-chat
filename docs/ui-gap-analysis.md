# UI / 展示 / 交互缺口清单

> 按「现在能看到什么 → 缺什么 → 属于哪一阶段」整理。
> 统计基准：P0-a 完成，代码截止 commit 079f0a8（含本仓库 + relay 仓）。
> 2026-09-02 回写：批 1（UI 基础设施 + 视觉重设计）与批 2（P0 收尾：编辑/草稿/空态/时间分组）已完成，本清单相应条目已更新。
> 2026-09-07 回写：批 3（P1 首批：群聊会话壳、附件上传壳、host 群镜像聚合、bob group/groupsend）与收尾项（48 处文档断链修复、能力表 partial 化）完成；本次一并订正 §1/§2/§3/§4/§8 中批 1–3 已落地而此前漏更新的条目。
> 阶段标注：🔴 P0-b 必须补齐 / 🟠 P1 / 🟡 P2 / 🟢 P3 / 🔵 P4

---

## 一、现状：已有的展示与交互

### 1.1 设置面板（`StatusSection`）
- ✅ dsh-chat 分区已注册，展示**能力状态表**：插件装载、错误码集合、本地持久化、授权判定等模块状态（已就绪绿 / 部分实现橙 / 未装载灰）
- ✅ 头部显示「协议版本 v1 · 数据库 schema v1」
- ✅ 「打开配置文件」「导出诊断信息」「打开 DSH 终端」三个按钮（DSH 原生按钮，非本插件实现）

### 1.2 会话页右侧抽屉（`ChatDrawer` + `ChatSection`）
- ✅ 会话头部「聊天」按钮，未读角标，点击右侧滑出抽屉（默认 520px，可拖 300–900px）
- ✅ 抽屉头部：标题、可见范围选择器（`VisibilityPicker`）、恢复默认宽度、关闭
- ✅ 布局：≥640px 并排（左列表 + 右消息），<640px 单栏钻取（列表 → 消息 + 返回条）
- ✅ Tab 切换：「会话」/「通讯录」两栏

#### 1.2.1 会话列表（`ConversationList`）
- ✅ 会话条目：在线状态点（实/空/无，配 `title`/`aria-label`）、名字、预览、时间、未读角标（>99 折 99+）
- ✅ 选中高亮、空态文案「还没有会话」
- ✅ 相对时间与日历日分组（批 2：今天/昨天/M月D日/跨年带年份；批 3：统一北京时间 UTC+8）

#### 1.2.2 消息视图（`MessageView`）
- ✅ 左右分侧气泡，作者名 + 时间
- ✅ 撤回占位「[已撤回]」、已编辑标「已编辑」（撤回的不重复标已编辑）
- ✅ 本端发送的消息显示投递态：待发送 / 服务器已接收 / 发送失败（失败给重试按钮）
- ✅ 事件流状态条：已连接（不显示）/ 正在重连 / 连接已断开 / 同步分叉（停止自动重发）
- ✅ 失败重试按钮只对 retryable 错误出现
- ✅ 空态「还没有消息」
- ✅ 消息行头像（批 1 生成式头像：首字 + hash→色）
- ✅ 右键菜单（批 1 `DropdownMenu`：复制、撤回；批 2 增编辑入口；转发/回复/引用/删除仍缺）
- ✅ 消息操作入口（批 2 内联编辑 + 撤回二次确认 Dialog）
- ❌ **没有已读/已送达回执**（P0 文档明确不做，但界面没有任何对应告知）
- ⚠️ 消息正文仍是纯文本：@提及、表情反应、引用、代码块均无；附件只有上传壳（批 3，发送前门禁，不能真发）

#### 1.2.3 输入框（`Composer`）
- ✅ Enter 发送 / Shift+Enter 换行，输入法组合期间不发送（`isComposing`）
- ✅ 字数按字素簇计（`Intl.Segmenter`），上限 8000，剩余 ≤200 显示计数，超限时变红
- ✅ 发送先清空输入框，失败回滚并显示错误；发送中按钮变「…」
- ✅ 发送失败错误由 `presentError` 给出文案
- ✅ 草稿保存（批 2：localStorage 按会话，发送清空/失败回滚，列表带「草稿」标记）
- ⚠️ 附件上传壳（批 3：选择/拖拽暂存、图片预览与移除）；@提及、表情选择、粘贴文件入口仍无
- ✅ 输入框自适应高度（批 1）
- ✅ 发送失败重试已接通（批 1：`onRetry` 复用幂等 `messageId`）

#### 1.2.4 通讯录（`DirectoryPanel`）
- ✅ 搜索框，按名字过滤
- ✅ 待处理请求置顶（「待处理请求（N）」+ 接受按钮）
- ✅ 四种状态按钮：发消息 / 等待通过（禁用）/ 在上方待处理 / 加联系人
- ✅ 空态：「这个组织里还没有别人」/「没有匹配的同事」
- ✅ 拒绝按钮（接 `/contacts/reject`，批 1）
- ✅ 移除联系人入口（接 `/contacts/remove`，批 1）
- ❌ **没有组织/工作区/项目三级结构**（现在是扁平成员列表）
- ❌ 自己不在列表里，但没有「我的资料」入口

#### 1.2.5 开户面板（`EnrollmentPanel`）
- ✅ 邀请码、显示名、设备名三字段，设备名按平台猜默认值
- ✅ 三字段非空才能提交，提交中禁用按钮，输入即清上次错误
- ✅ 失败只显示「这张邀请码无法使用」（不区分已过期/已消费/不存在）
- ✅ 退出登录（批 1 设置面板：确认 → `/identity/sign-out` → 刷新）；设备切换/多设备管理仍无（P0-b）
- ✅ 账号信息（批 1 `BasicSettings`：模式徽标、账号 ID 复制、设备 ID、版本胶囊）；设备列表仍缺（P0-b）

---

## 二、🔴 P0-b 必须补齐的 UI（文档明确要求）

| 模块 | 需补内容 | 文档出处 |
|---|---|---|
| **第二因素 (2FA)** | 设置里加「账号安全」分区：TOTP 二维码/密钥展示、6 位验证码输入、备用码展示（一次性，复制后警告）、已登记设备列表、撤销入口 | §8.1 步骤 1–2；P0-b 要求未登记 2FA 时**只能访问账号安全设置**，其余入口需锁定 |
| **组织强制策略提示** | 未满足策略时（未登记 2FA、密码强度、版本不兼容）在抽屉顶部给出**占据布局的警告条**（不是 toast），指向解决入口 | §8.1 步骤 2；presentError 已有 `conditional` 状态，但界面未渲染前置条件入口 |
| **恢复 (RecoveryKit)** | 账号安全里：生成恢复包按钮 → 展示守护人拆分方案 → 下载/保存引导 → 「我已保存」确认；恢复流程入口（从其他设备/新设备登录时触发） | §8.1 步骤 3 |
| **在线可见范围** | ✅ `VisibilityPicker` 已接通 API（批 1）：调 `/presence/visibility`（读）+ `/presence/visibility/set`（写），乐观更新 + 失败回滚，handler/持久化表齐全。三档可见范围（everyone/shared_scopes/hidden）与 §9.1 设计一致。**注意**：若需求要四档（在线/空闲/离线/对谁可见），则是新增缺口——当前只做「对谁可见」 | §8.1 步骤 4 |
| ~~本地搜索~~ | ✅ 已完成（本轮 P0-b-07）：会话列表搜索框过滤标题/预览 + 当前会话消息正文高亮（撤回消息排除）；跨会话消息搜索属 P1 服务端搜索 | §8.1 步骤 5 |
| **协议协商失败态** | 当 `PROTOCOL_VERSION_UNSUPPORTED` 时，抽屉替换为升级提示页（不是一个小 banner），告知版本不兼容、提供升级指引 | §8.1 步骤 6 |
| ~~发送失败重试接通~~ | ✅ 已完成（批 1）：`onRetry` 接到 `ChatSection`，复用幂等 `messageId` 重发 | — |

---

## 三、🟠 P1 阶段 UI（群聊、资源库、附件、服务端搜索）

| 模块 | 需补内容 |
|---|---|
| **群聊** | ✅ 壳已落地（批 3）：群类型会话（`kind`/`memberCount`）、方形群头像与成员徽标、host 群镜像/聚合（S4b）、bob `group`/`groupsend` 播种与组播（S2/S4c）。仍缺：UI 群发送入口、群设置、@提及补全、加入/离开群系统消息、入群验证 |
| **附件** | ✅ 上传壳已落地（批 3）：选择/拖拽暂存、图片预览与移除、发送前如实门禁。仍缺：真实上传与进度条、下载入口、大小/过期提示、撤回连带、粘贴入口 |
| **消息操作** | 右键/长按菜单：复制、编辑（本人 10 分钟内）、撤回、引用、转发、回复、删除（本地删除 vs 撤回）、多选 |
| **历史授权** | 首次进入会话/群时，若需要授权才能看历史，弹出授权卡（不是空列表） |
| **资源库** | 新增 Tab 或独立分区：文件/文档列表、按项目/会话聚合、上传入口、预览（图片直显，其它给图标）、权限继承显示 |
| **仓库绑定** | 项目设置里绑定 GitHub/GitLab 仓库入口，绑定后工作项里显示关联 PR/commit |
| **服务端搜索** | 跨会话/跨项目搜索：搜索入口独立于本地搜索，结果分页、分类（消息/文件/工作项/成员）、按相关性排序 |
| **工作项 UI**（目前只有 API，没有界面） | 工作项列表（指派给我/我创建/全部）、详情页（状态、截止时间、依赖关系、评审关口）、评论、状态流转按钮 |
| **通知中心**（目前只有 API，没有界面） | 通知收件箱独立入口、按类型分组（工作项/邀请/审核/系统）、聚合显示、全部已读、跳转到对应上下文 |
| **多设备/跨主机界面** | 设备列表（我的设备/活跃设备）、远程下线、设备名称编辑、新设备登录告警条 |
| **头像** | ✅ 生成式头像（批 1）与方形群头像（批 3）已落地；头像上传入口仍缺 |
| **相对时间** | ✅ 已落地（批 2「刚刚/X 分钟前/今天/昨天/M月D日」+ 批 3 统一北京时间）；跨时区时区标识仍缺 |
| **会话列表聚合端点 UI 对齐** | `/conversations` 已含群行合并（批 3 S4b）；草稿标记已做（批 2）；lastMessage 类型、pin、mute 呈现仍缺 |

---

## 四、🟡 P2 阶段 UI（协作会话、沙箱执行、Bot、线程与转发）

| 模块 | 需补内容 |
|---|---|
| **协作会话** | 会话里能看到 AI 协作入口：选中上下文 → 发起协作 → 候选产物卡片（采纳/拒绝/编辑）→ 采纳后插入消息 |
| **沙箱执行 UI** | 命令执行的输出流展示（不是静态块）、终止按钮、产物区、权限提示 |
| **群 Bot** | 群里添加/移除 Bot、Bot 消息的特殊样式（带头像/标识）、Bot 权限提示 |
| **线程（Thread）** | 消息下「回复 X 条」入口 → 侧滑或展开线程视图、线程独立未读计数 |
| **转发** | 转发目标选择器（搜索联系人/群）、转发附言、转发引用样式（原消息卡片+转发者） |
| **表情反应** | 消息下方反应条、常用表情、hover 显示谁点了什么 |
| **草稿** | ✅ 已落地（批 2 提前完成：localStorage 按会话，切换不丢，列表预览带「草稿」标记） |
| ~~SSE 实时订阅接通~~ | ✅ 已完成（#37）：`useEventStream` 订阅 `/api/chat/events`，自管退避重连 + 断线按游标补拉，流状态如实显示。已读回执即时更新仍缺；输入中状态见下行 |
| **输入中状态** | 对方正在输入时在会话列表和消息区显示「XX 正在输入…」 |

---

## 五、🟢 P3 阶段 UI（团队版、治理、插件目录、分析）

| 模块 | 需补内容 |
|---|---|
| **团队版限制提示** | 配额接近上限时给预警、超额时在输入/上传点给出明确拒绝文案（不是报错） |
| **组织治理/管理后台** | 成员管理（角色调整、禁用、移除）、工作区/项目 CRUD、邀请码生成与管理、审计日志浏览页 |
| **插件目录** | 独立页浏览/安装/卸载组织插件、权限申请流程 |
| **成本大屏/排行** | 用量统计（不读私聊）、成员/项目维度排行、时间范围选择、导出 |
| **唯一所有者转让** | 注销/离开前强制转让流程 UI |
| **设置页重构** | 把设置从 dsh-chat 分区里独立出来：账号安全、通知、隐私（在线可见范围）、外观、关于 |

---

## 六、🔵 P4 阶段 UI（企业版、分片、E2EE、审计归档）

| 模块 | 需补内容 |
|---|---|
| **E2EE 标识** | 会话/消息级加密锁图标、密钥指纹核对（扫码/数字比对）、密钥轮换提示 |
| **分片/多组织切换** | 组织切换器（多组织身份）、跨组织的会话分组 |
| **企业目录同步状态** | 管理员页：同步进度、冲突列表、手动触发 |
| **审计归档 UI**（管理员） | 审计查询（按人/时间/事件类型）、导出、哈希链校验状态 |
| **KMS/密钥管理**（管理员） | 密钥列表、轮换计划、HSM 状态 |
| **降级矩阵呈现** | E2EE 不支持的客户端/功能显式灰化并说明原因 |

---

## 七、🔧 全局/基础设施类 UI 缺口（不绑阶段，但越早补越好）

| 类别 | 缺口 |
|---|---|
| **空状态/加载/错误态** | 目前 5 个主要面板（会话、消息、通讯录、开户、状态）有，但：工作项、通知、搜索、资源、设置子页都没有；错误文案统一走 `presentError` 但 `conditional` 类错误没渲染「解决入口」按钮 |
| **可达性 (a11y)** | 焦点环样式与焦点陷阱（Dialog）已落地（批 1）；剩：键盘导航走查（↑↓ 选会话、Tab 顺序）、屏幕阅读器实测、颜色对比度校验 |
| **暗色模式** | ✅ 已落地（批 1 design tokens 浅/暗两套，随 DSH 主题） |
| **国际化** | 全部文案硬编码中文，无 i18n 框架；README 有英文版但 UI 没有 |
| **loading 骨架** | ✅ 已落地（批 1 `Skeleton` 组件） |
| **toast/通知系统** | ✅ 已落地（批 1 `Toast` 组件：去重 / FIFO / `role` 完备） |
| **快捷键** | 除了 Esc 关抽屉、Enter 发送，没有其他全局/局部快捷键（Ctrl+N 新会话、Ctrl+K 搜索、↑编辑上一条…） |
| **右键菜单组件** | ✅ 已落地（批 1 `DropdownMenu`：右键 + 键盘导航） |
| **对话框组件** | ✅ 已落地（批 1 `Dialog`：焦点陷阱 + a11y + 滚动锁） |
| **虚拟滚动** | 消息列表与会话列表现在全量渲染，群聊/长历史下会卡（P1 前必须补） |
| **图片/文件预览组件** | Composer 已有本地图片预览（批 3，暂存态）；消息侧通用预览组件（图片 lightbox、视频 player、文件下载卡）仍缺 |

---

## 八、📋 建议落地顺序（视觉→交互→功能的合理切片）

**第一批：把 P0-b 的 UI 壳子先搭起来**（因为后端 P0-b 还没写，前端可以先做静态壳）
1. 账号安全分区骨架（2FA、RecoveryKit 入口的位置与跳转）— ⬜ 待 P0-b 后端
2. VisibilityPicker 接通 `/presence/visibility/set` — ⬜ 壳在，未接通
3. 本地搜索框 UI — ✅ 批 1（`LocalSearch`：会话标题/预览过滤 + `<mark>` 高亮；正文级属 P0-b）
4. 发送失败重试接通 `onRetry` — ✅ 批 1
5. 通用组件：Dialog、Toast、DropdownMenu — ✅ 批 1

**第二批：补体验缺陷**
6. 头像（生成式）、相对时间 — ✅ 批 1/批 2（批 3 统一北京时间）
7. 草稿保存 — ✅ 批 2
8. Composer 自适应高度 — ✅ 批 1
9. 消息右键菜单：复制、编辑、撤回 — ✅ 批 1/批 2
10. 拒绝/移除联系人按钮 — ✅ 批 1

**第三批：P1 大项**
11. 附件上传 + 预览 — 🔶 上传壳已落地（批 3）；真实上传/下载待 P1 后端
12. 工作项 UI、通知中心 UI — ⬜ 未开始
13. 群聊类型会话 + @提及 — 🔶 群会话壳与 host 镜像已落地（批 3）；UI 群发送、@提及未做
14. 服务端搜索 — ⬜ 未开始

---

*本文档基于对 `packages/chat/client/src/client/*.tsx` 的逐文件扫描 + host 路由表核对 + docs/archive/04-roadmap/03-iteration-plan.md 的阶段要求整理。最后更新：2026-09-07。*
