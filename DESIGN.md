# dsh-chat 设计文档

> 面向自建团队、受管团队与企业组织的 DSH Web 协作平台。
> 本文档是实现、评审与验收的唯一依据；所有边界声明与拒绝语义均为强约束，不是建议。

## 阅读指引

文档分为九篇。若你只想了解产品是什么，读第一篇即可；若要开始实现，第二篇与第三篇是必读的骨架；安全评审集中在第六篇；排期与验收集中在第八篇。

| 篇 | 主题 | 适合谁先读 |
|---|---|---|
| [一](#一定位与边界) | 定位与边界 | 所有人 |
| [二](#二架构与插件模型) | 架构与插件模型 | 架构、后端、前端 |
| [三](#三身份组织与权限) | 身份、组织与权限 | 后端、安全 |
| [四](#四协作能力) | 协作能力（消息、工作项、资源、协作会话、Bot） | 产品、后端、前端 |
| [五](#五服务端运行面) | 服务端运行面（部署分层、投递、持久化） | 后端、运维 |
| [六](#六安全与合规) | 安全与合规 | 安全、法务、运维 |
| [七](#七可观测性与运维) | 可观测性与运维 | 运维、SRE |
| [八](#八交付计划与验收) | 交付计划与验收 | 项目管理、QA |
| [九](#九契约与规范附录) | 契约与规范附录（错误码、术语、编码规范） | 所有工程师 |

阅读约定：文中「必须」「不得」「绝不」表示强约束，违反即为缺陷；「默认」表示可由版本化配置覆盖的起始值。所有以反引号标注的标识符、状态与错误码均在第九篇有唯一定义。

---

## 目录

**[一、定位与边界](#一定位与边界)**
1. [产品定位](#1-产品定位) · 2. [目标用户](#2-目标用户) · 3. [能力边界与不做清单](#3-能力边界与不做清单)

**[二、架构与插件模型](#二架构与插件模型)**
4. [三层总体架构](#4-三层总体架构) · 5. [客户端结构与呈现约定](#5-客户端结构与呈现约定) · 6. [插件化架构](#6-插件化架构)

**[三、身份、组织与权限](#三身份组织与权限)**
7. [身份与设备注册](#7-身份与设备注册) · 8. [第二验证因素](#8-第二验证因素) · 9. [账号设置与组织切换](#9-账号设置与组织切换) · 10. [多设备同步](#10-多设备同步) · 11. [组织、工作区与角色](#11-组织工作区与角色) · 12. [组织类型、容量与订阅](#12-组织类型容量与订阅)

**[四、协作能力](#四协作能力)**
13. [联系人与群聊](#13-联系人与群聊) · 14. [消息模型](#14-消息模型) · 15. [消息交互功能范围](#15-消息交互功能范围) · 16. [附件与内容授权](#16-附件与内容授权) · 17. [工作项与通知](#17-工作项与通知) · 18. [评审与评论](#18-评审与评论) · 19. [共享存储与选择性共享](#19-共享存储与选择性共享) · 20. [协作会话与开发交接](#20-协作会话与开发交接) · 21. [搜索与索引](#21-搜索与索引) · 22. [仓库与 AI 辅助开发记录](#22-仓库与-ai-辅助开发记录) · 23. [群内公共 Bot](#23-群内公共-bot) · 24. [公共插件与工具目录](#24-公共插件与工具目录) · 25. [仪表盘、报告与排行](#25-仪表盘报告与排行)

**[五、服务端运行面](#五服务端运行面)**
26. [服务端闭环与写入协议](#26-服务端闭环与写入协议) · 27. [L0 至 L3 渐进部署](#27-l0-至-l3-渐进部署) · 28. [可靠投递流程](#28-可靠投递流程) · 29. [本地与 relay 持久化](#29-本地与-relay-持久化) · 30. [性能、分片与限流](#30-性能分片与限流)

**[六、安全与合规](#六安全与合规)**
31. [标识、租户与会话隔离](#31-标识租户与会话隔离) · 32. [授权链、强制确认与黑名单](#32-授权链强制确认与黑名单) · 33. [连接、外部网络与 SSRF 防护](#33-连接外部网络与-ssrf-防护) · 34. [风险管制与账号接管防御](#34-风险管制与账号接管防御) · 35. [加密与密钥](#35-加密与密钥) · 36. [缓存、保留与恢复](#36-缓存保留与恢复) · 37. [审计事件模型](#37-审计事件模型) · 38. [账号注销、导出与合规](#38-账号注销导出与合规) · 39. [安全规范清单](#39-安全规范清单)

**[七、可观测性与运维](#七可观测性与运维)**
40. [可观测性、SLO 与容量目标](#40-可观测性slo-与容量目标) · 41. [协议版本协商与升级顺序](#41-协议版本协商与升级顺序)

**[八、交付计划与验收](#八交付计划与验收)**
42. [关键操作的成功与失败状态](#42-关键操作的成功与失败状态) · 43. [最小可运行骨架](#43-最小可运行骨架) · 44. [迭代计划 P0–P4](#44-迭代计划-p0p4) · 45. [测试与验收策略](#45-测试与验收策略)

**[九、契约与规范附录](#九契约与规范附录)**
46. [错误码目录](#46-错误码目录) · 47. [术语表](#47-术语表) · 48. [编码规范](#48-编码规范) · 49. [国际化、时区与无障碍](#49-国际化时区与无障碍) · 50. [开放决策](#50-开放决策)

---

# 一、定位与边界

本篇回答三个问题：这个产品是什么、给谁用、明确不做什么。后续所有技术决策都以本篇的边界声明为准绳。

## 1. 产品定位

`dsh-chat` 是 **DSH Web 中面向自建团队、受管团队和企业组织的协作平台**。

聊天是协作入口之一；组织成员还能在工作区和项目内分派工作、共享文件与 AI 产物、交接开发会话、使用经过审核的公共插件和 Bot，并查看与其权限相符的协作数据。

**它不是微信集成、微信替代品、微信协议实现或微信账号桥接。**

> **核心价值**：让人、项目资源和 DSH AI 工作流处于同一受控组织中。**聊天记录和私人 AI 会话严格分开**；用户只能主动选择内容提交给 AI，组织共享内容也**必须**经过明确授权。

## 2. 目标用户

产品面向三类用户：自行部署基础设施的个人/班级/私有团队，购买受管服务的团队，以及需要目录同步、审计和治理能力的企业组织。

每位成员在自己的 DSH Web profile 中运行 dsh-chat 插件；组织服务端决定身份、成员关系、套餐、资源权限、公共工具和组织级数据。

## 3. 能力边界与不做清单

**P0 到 P3 的 relay 仍可能读取明文消息**：传输中使用 TLS、在服务器侧加密存储投递和协作数据，但服务器持有解密能力。P4 的端到端加密使用独立审计过的密钥生命周期，使 relay 只路由密文和最少元数据。

早期版本**不提供**账号发现、移动推送、音视频通话，或可不经授权读取和发送聊天内容的 AI。

**DSH 智能体会话日志不是聊天历史。**

> **AI 摄入红线**：只有用户明确选择并触发 AI 操作的内容才能进入 AI 请求；这些内容在对模型可见前**必须**通过正常的 DSH 会话机制记录。

以下边界在全文反复出现，集中声明以避免实现期误解。**这八条是评审时最容易被实现者误解的地方**：

| 边界 | 含义 |
|---|---|
| **撤回 ≠ 抹除** | 不承诺擦除已截图、导出、复制、设备缓存或经历史授权下载的内容 |
| **撤销共享 ≠ 删除** | 只阻止后续读取，不删除已下载的本地副本 |
| **注销 ≠ 抹除他人副本** | 匿名化保留他人已合法接收的协作事实 |
| **执行成功 ≠ 产物被接受** | 候选产物需有写权限成员显式确认 |
| **通知已读 ≠ 任务已知晓** | 签收状态独立于通知状态 |
| **在线状态 ≠ 可打扰** | 只表达 host 存活，不推断阅读或输入 |
| **服务端加密 ≠ 端到端加密** | P0–P3 relay 可读明文 |
| **应用层删除 ≠ 约束基础设施管理员** | 自建部署的运维者可直接操作底层存储 |

---

# 二、架构与插件模型

本篇定义组件边界与扩展模型。核心原则是凭证按层隔离、能力按插件拆分，且不存在特权内核。

## 4. 三层总体架构

**浏览器只负责呈现和交互，不持有组织秘密、relay 凭证或设备私钥。**

DSH host 负责本地持久化缓存、保存设备凭证、运行 relay 客户端、发送在线心跳、安排重试，并提供浏览器使用的同源 API。

relay 是组织协作的权威运行面，由 dsh-chat 插件组合而成。身份、组织、成员、消息、资源、通知、仓库、协作执行、Bot、插件目录、分析和计费各自拥有服务接口与持久事件；**任何单一功能都不成为不可替换的 relay 内核**。

```text
DSH Web 客户端
  -> 同源 /api/chat、/api/organization 和事件流
DSH host 插件
  -> 本地 SQLite 缓存、加密媒体目录和 DSH 私人会话
  -> 向 relay 发起认证后的 HTTPS 请求和在线心跳
relay
  -> 持久化账号、组织、项目、群、队列、资源、仓库、插件、通知和分析元数据
  -> 异步处理审核、通知、Bot、扫描、仓库校验和报表任务
```

三层的凭证归属是硬边界：

| 层 | 持有 | 明确禁止 |
|---|---|---|
| 浏览器 | 仅呈现与交互状态 | **不持组织秘密、relay 凭证、设备私钥**；不能直接携带 relay access token |
| host | 本地数据库、设备凭证、relay 客户端 | **是浏览器访问组织的唯一入口** |
| relay | 组织权威数据与策略判定 | **不保存设备私钥**；不保存可直接恢复账号的明文秘密 |

Web 客户端**必须**作为 DSH client 插件实现：通过 `ctx.slots.register` 组合 UI，用已声明的 store 保存视图状态，并使用 DSH 主题 token 与 CSS Modules。

客户端**不得**向宿主页面注入未受管理的 DOM 节点，也**不得**依赖 `MutationObserver` 修复宿主重渲染。

host 路由通过 `ctx.webServer.register` 注册在 `/api/chat` 和 `/api/organization` 下，并拥有完整的请求生命周期，包括浏览器事件流。

## 5. 客户端结构与呈现约定

客户端按功能域拆分为可独立卸载的 slot 贡献：会话列表、消息视图、工作项、资源库、搜索、通知收件箱、组织与账号设置、仪表盘。每个域声明自己的 store 与路由段，卸载时释放订阅、事件流与定时器，不残留全局单例。

**视图状态与持久状态分离**：滚动位置、折叠状态、草稿编辑器焦点等属于设备本地的视图状态，放入已声明的 DSH store；消息、工作项、授权与游标属于持久状态，只能来自 host 数据库，**客户端不做权威缓存也不在浏览器中重算权限**。

> **界面显示的可用操作来自 host 返回的能力描述，而不是客户端自行推断角色。** 隐藏入口只是呈现优化，服务端仍对每次调用完整鉴权；客户端**不得**因为已隐藏入口而省略错误处理。

事件流断开、组织切换、权限修订变化和 `sync_diverged` 都是客户端**必须显式呈现**的状态，不能表现为静默停止刷新。离线时界面区分**“本地已保存待发送”“服务器已接收”和“终态失败”三种状态，绝不把未确认内容显示为已送达**。

错误呈现按错误码的可重试性分级：`retryable` 提供重试入口，`conditional` 说明需要先满足的前置条件并给出对应操作入口，`terminal` 说明原因且不提供重试按钮。客户端**不得**把 `NOT_FOUND_OR_FORBIDDEN` 渲染成“对象不存在”这类推断性描述。

大列表使用虚拟滚动与分页游标，不一次性装载整段群历史或资源目录；图片与附件按需加载并遵守下载授权，预览失败显示明确终态而不是无限加载。

## 6. 插件化架构

dsh-chat 遵循 DSH 的“一切皆插件”模型。除 `@dsh-chat/contract` 这一纯类型、schema 与协议库外，所有业务能力都以 Cordis 插件注册到 `ctx`；**没有需要直接修改的特权聊天内核**。插件用 `ctx.effect()` 或 `ctx.on()` 注册服务、事件监听、路由、后台任务和 UI 节点，并在卸载或被撤销时释放注册、连接、租约和任务。

每个能力均由三种角色组成：**服务定义**插件声明稳定接口、配置和错误语义；**服务提供者**插件实现 SQLite、PostgreSQL、Redis、对象存储、SMTP、KMS 或沙箱等后端；**消费者**插件通过接口调用能力，例如消息插件消费成员授权和通知服务，仓库插件消费受控出站服务。**消费者不得导入某个提供者的数据库模型或绕过服务调用。**

插件间的持久协作使用领域事件和事务 outbox，**不用跨插件共享可变内存**。会影响 DSH 模型输入的消息、资源摘录、协作上下文或 Bot 上下文**必须**同时写入对应的 DSH `SessionEvent`，以便重放、审计和权限复核；普通 UI 提示可以是可重建的实时事件。

### 6.1 能力与提供者矩阵

| 插件能力 | 服务定义与主要职责 | 典型提供者 | 消费者与持久事件 |
|---|---|---|---|
| 身份与设备 | `ChatIdentity`：注册、登录、设备证明、恢复、撤销与风险状态 | L1 SQLite；L2 PostgreSQL/Redis；L3 企业身份与 KMS 适配器 | 所有写入插件；`device_registered`、`device_restricted`、`device_revoked` |
| 组织与授权 | `ChatOrganization`：组织、工作区、项目、成员、角色、ACL、套餐与确认挑战 | L1 SQLite；L2 PostgreSQL；L3 目录同步提供者 | 消息、资源、Bot、分析；`membership_changed`、`policy_changed` |
| 在线状态 | `ChatPresence`：心跳、可见范围与订阅 | 本地/Redis 心跳提供者 | 联系人、群列表、通知；`presence_changed` |
| 消息与群日志 | `ChatMessaging`：私聊队列、群日志、编辑、撤回、ACK、游标和水位 | SQLite 队列；PostgreSQL/outbox 分区队列 | 客户端同步、通知、Bot；`message_accepted`、`message_edited`、`message_revoked` |
| 内容与资源 | `ChatContent`：上传预约、`BlobObject`、资源版本、扫描、下载授权和清理 | 本地目录；S3/MinIO；企业 KMS 加密存储 | 消息附件、共享、预览、Bot；`resource_available`、`resource_quarantined` |
| 通知 | `ChatNotification`：收件箱、偏好、邮件、SSE 提示和任务签收通知 | 本地 outbox；SMTP；企业通知适配器 | 工作项、消息、风险、插件目录；`notification_created`、`notification_read` |
| 协作与执行 | `ChatCollaboration`、`ExecutionProvider`：共享、交接、执行租约、沙箱、候选产物与接受流程 | 本地禁网进程；受控容器；企业隔离执行服务 | 工作项、资源、仓库；`execution_started`、`execution_finished`、`artifact_accepted` |
| 工作项与评审 | `ChatWorkItem`：工作项状态机、依赖、签收、评审与评论 | L1 SQLite；L2 PostgreSQL | 通知、协作、分析；`work_item_changed`、`review_requested`、`review_completed` |
| 仓库与出站 | `ChatRepository`、`EgressService`：绑定、验证、webhook、提交归因和受控网络 | L1 不提供；L2 allowlist HTTP；L3 出口代理与专用连接器 | 项目群、分析、自动化；`repository_verified`、`code_update_verified` |
| Bot 与公共工具 | `ChatBot`、`ChatPluginRegistry`：Bot 调度、插件审核、发布、能力租约与撤销 | 本地禁用；L2 隔离 worker；L3 KMS/沙箱/审计提供者 | 群、资源、模型、工作流；`bot_invoked`、`plugin_revoked` |
| 分析与计费 | `ChatAnalytics`、`ChatBilling`：用量归集、预算、报告、同层级排行和套餐限制 | SQLite 聚合；PostgreSQL 聚合；企业数据仓库适配器 | 仪表盘、管理员策略；`usage_aggregated`、`budget_exceeded` |
| 搜索与索引 | `ChatSearch`：索引管线、可搜范围、查询授权复检与索引失效 | L1 host 本地索引；L2 服务端索引提供者；L3 分片索引与设备侧索引 | 消息、资源、工作项、插件目录；`index_updated`、`index_suppressed` |
| 审计 | `ChatAudit`：审计事件写入、哈希链、查询授权与归档导出 | L1 仅追加表；L2 每日锚点；L3 逐条链与外部归档 | 所有写入插件；`audit_recorded`、`audit_chain_broken` |
| 合规与生命周期 | `ChatCompliance`：数据导出、账号注销、组织删除、宽限期与保留执行 | L1 本地导出；L2 对象存储导出；L3 法规归档与法律保留 | 身份、组织、内容、分析；`export_ready`、`account_anonymized`、`organization_purged` |

插件的配置**必须**通过 schema 验证，含 `OrganizationId` 范围、依赖服务、权限、资源上限、保留期和停机行为。启动顺序由 bundle 配置的依赖关系决定：先加载身份、组织和审计，再加载消息/内容等消费者；**缺少必需提供者时 profile 加载失败，不允许静默降级**。可选能力**必须**显式显示为未安装或 `NOT_IMPLEMENTED`。

组织公共插件与系统核心插件使用**不同信任级别**。系统核心插件随 dsh-chat bundle 安装、只能由部署管理员替换并受部署审计；组织公共插件由组织审核后供成员启用，始终运行在能力租约和成员 ACL 内，**不能替换身份、授权、审计、出站或密钥插件**。

---

# 三、身份、组织与权限

本篇定义“谁是谁”以及“谁能做什么”。授权模型是双层的：角色给出默认能力，资源 ACL 决定实际操作。

## 7. 身份与设备注册

**身份是账号，不是客户端随机生成的展示地址。**

用户提交管理员签发的一次性注册邀请码后，relay 创建不可预测的 `AccountId`。

DSH host 在本地生成 Ed25519 签名密钥对和 X25519 密钥协商密钥对，并在注册时把公钥、设备名称和公钥指纹与邀请码提交给 relay。

relay 在账号和 `DeviceId` 下保存公钥、SHA-256 指纹、设备状态和首次/最后验证时间，并为该设备签发短期 access token 和可轮换的 refresh token。

> **设备私钥永远不上传至 relay。** relay 不保存“与客户端相同的秘密”，而是保存公钥和指纹；设备**必须**以私钥签名证明自己仍持有与该指纹匹配的密钥。

组织同时维护 relay TLS 证书/公钥指纹。host 首次经管理员确认或受信任注册包固定该指纹；**后续连接若证书、公钥或证书链意外变化，立即进入 `server_identity_mismatch`**，停止组织数据同步并要求管理员确认受签名的轮换记录。

### 7.1 请求签名与时间偏移

每个认证请求都携带 access token，以及设备签名私钥对请求方法、路径、请求体摘要、时间戳、nonce、`DeviceId` 和目标组织签名生成的证明。

relay 拒绝过期时间戳、重复 nonce、未注册或被限制设备、错误的 relay 指纹、以及与凭证账号不一致的声明发送者。

设备签名时间戳采用可配置容忍窗口，**默认正负 5 分钟**。relay 检测到超窗但签名、nonce 和设备指纹其他条件正确时，返回 `TIME_SKEW`、签名的服务器时间和允许窗口，**不把它混同为认证失败**。

host 用服务器签名时间计算仅用于请求签名的临时偏移量，不擅自修改操作系统时钟；同时提示用户校正系统时间。临时偏移有短期有效期，超过最大偏移或反复出现时要求重新认证或管理员处理，避免攻击者借时间响应扩大重放窗口。

用户分享由 `AccountId` 派生的公开联系人地址；**它只能寻址，不能认证**。

### 7.2 恢复路径

添加第二设备、恢复账号、撤销设备或轮换恢复材料优先由已经授权的设备确认；这不是自建组织唯一的恢复路径。

**首次注册时必须创建 `RecoveryKit`。** 自建组织用户在本地生成恢复密钥，以用户选定的恢复口令加密导出，并可选择把恢复密钥按阈值拆分给 2 至 5 名守护人，例如“2/3 守护人同意”。relay 只保存恢复公钥、阈值策略、守护人账号和恢复事件，**绝不保存可直接恢复账号的明文秘密**。

唯一设备丢失时，用户可以用恢复包加恢复口令，或用满足阈值的守护人批准，向 relay 注册新的设备公钥。relay 进入 `recovery_pending`，冻结旧设备、发送安全通知、等待恢复证明和必要的邮箱/密码或企业身份验证；证明通过后以新 `DeviceId` 恢复账号访问。

自建组织的第一个组织所有者**必须**在创建组织时导出离线组织恢复包，或指定至少两名恢复守护人；**未完成其中任一项不能完成组织激活**。这样管理员本人丢失唯一设备时仍有技术恢复路径。

> **账号恢复与内容恢复是两个不同承诺。** P0 到 P3 恢复账号后可读取仍在服务器保留期内的非端到端加密数据；P4 的端到端加密历史只有在用户启用并保存端到端恢复密钥或可信守护人恢复策略时才能解密，**未配置恢复策略的旧密文不可恢复，这是明确的安全取舍**。

**共享域口令绝不能单独用于注册、替换或恢复其他人的账号。**

## 8. 第二验证因素

设备私钥签名是主要认证手段，第二验证因素用于保护设备之外的高风险路径：新设备注册、账号恢复、密码或认证方式变更、安全中心处置、组织所有权转让和企业单点登录降级场景。

支持的因素类型为 `webauthn`（平台或漫游认证器，优先）、`totp`（RFC 6238，30 秒步长，容忍前后各一步）和 `recovery_code`（一次性备用码）。**短信与邮件验证码不作为第二因素**；已验证邮箱只承担安全通知与一次性处置链接，不承担认证强度。

每个因素记录 `FactorId`、类型、标签、创建时间、最后使用时间、状态和登记设备。状态为 `pending_verification`、`active`、`suspended` 或 `revoked`；登记必须当场完成一次成功验证才能从 `pending_verification` 转为 `active`，避免登记了不可用的认证器。

登记、替换和删除第二因素本身属于高风险动作，需要 `ConfirmationChallenge` 与已授权设备确认。删除最后一个 `active` 因素时，若组织策略要求强制第二因素，则返回 `FORBIDDEN` 并提示先登记替代因素。

启用第二因素时**必须**一次性签发一组一次性备用码，**只展示一次并只保存哈希**。备用码消费即失效，剩余数量低于阈值时提示重新生成；**备用码不能用于替代设备签名**，只能在第二因素不可用时完成一次验证。

组织策略可要求成员必须登记第二因素，并按角色分级：组织所有者、组织管理员、账单管理员和拥有写仓库自动化权限的成员默认强制。未满足强制策略的成员在完成登记前只能访问账号安全设置，不能读写组织内容。企业版可要求 `webauthn` 并禁用 `totp`，也可将第二因素判定委托给企业单点登录，但委托后仍需记录认证强度以供审计。

第二因素验证失败按设备与账号分别限流，连续失败提升风险评分并可触发 `SecurityRiskEvent`；错误信息不区分“因素不存在”与“因素错误”。TOTP 已消费的时间步在容忍窗口内记录并拒绝重放。

> **第二因素与 `RecoveryKit` 是独立的两条路径，不能互相替代**：丢失全部设备时使用恢复包或守护人阈值，丢失第二因素时使用备用码或已授权设备重新登记。**两者同时丢失且未配置守护人时账号不可恢复**，注册流程**必须**明确告知这一取舍。

P0 支持 `totp` 与备用码，满足自建组织的最小安全要求。P3 加入组织强制策略与按角色分级。P4 加入 `webauthn`、企业单点登录强度传递和认证强度审计导出。

## 9. 账号设置与组织切换

账号设置页分为身份安全、已绑定设备、组织成员关系、当前组织、通知偏好和个人分析六部分。

账号可绑定公开账号名和经验证的邮箱；团队版和企业版可额外绑定单点登录身份。账号名用于登录和展示，不能作为消息认证依据。

邮箱验证使用短期、单次消费且只在 relay 保存哈希的验证 token。未验证邮箱不能接收安全处置链接，也不能作为设备恢复的唯一证明。

密码登录是可选认证方式。用户设置密码时，host 仅通过 TLS 把密码提交给 relay；**relay 只保存经过 Argon2id 和独立 salt 处理的验证值**，host、浏览器状态、日志和数据库缓存都**不得**保存明文密码。

使用设备密钥、单点登录或密码完成一次认证后，host 获取受限的设备 token；密码不会成为每次消息、文件或 Bot 调用的凭证。

用户可以在“已绑定设备”中查看每台设备的名称、指纹、最近活动时间、粗粒度地区/网络、风险状态和最后登录方式，并可撤销非当前设备。设备撤销立即使该设备 token、refresh token、长连接、上传预约、执行租约和组织访问缓存失效。

一个账号可以属于多个组织。host 把组织无关的账号与设备信息同组织级缓存、草稿、通知游标和偏好分开保存。

用户切换组织时，浏览器请求 host 更换当前 `OrganizationId`；host 校验该账号的成员状态为 `active`，加载该组织的权限、项目、群、资源入口、插件目录、预算可见范围和通知游标。

**切换组织不会把前一组织的消息、资源、私人会话、搜索索引或未发送草稿暴露到新组织。** 当前页面若持有旧组织资源，则先失效并重新授权。

用户退出组织后，host 删除该组织的凭证引用和可清理缓存；仍保留用户明确导出的个人副本和组织保留策略要求的审计事实。

### 9.1 在线状态

**在线状态表达 DSH host 是否仍在运行，不表示用户正在阅读、输入或愿意被打扰。**

每个活跃 host 用已认证设备定期发送心跳，relay 维护最近心跳和设备数。聚合状态为 `online`、`idle`、`offline` 或 `unknown`。

`online` 表示至少一台设备在心跳阈值内；`idle` 表示 host 仍存活但长时间没有用户交互；`offline` 表示所有设备超过离线阈值；`unknown` 表示 relay 无法可靠判定，例如刚加入组织、时钟异常或 relay 恢复期间。

> **在线状态是最终一致的提示信息，绝不用于推断已读、送达、是否可以打扰或是否自动把消息改为失败。**

用户可以按组织选择“所有成员可见”“仅项目/群成员可见”或“隐藏”；隐藏时仍向 relay 发送必要心跳以维持投递和安全，但对其他成员显示为 `unknown`。

## 10. 多设备同步

P0 即在 schema 中建立账户级 `AccountSyncState` 和设备级 `DeviceSyncState`，不把通知已读、群阅读位置或组织偏好永久限制在某台 host 的 SQLite 缓存中。

relay 为每个账户维护单调的 `AccountStateSeq` 变更流。通知、私聊和群的已读状态以“流 ID 到最高已读服务端序列”的稀疏水位映射保存；合并规则取同一流的最大序列，因此设备 A 已读后，设备 B 同步不会把已读通知重新变为未读。

每个设备提交状态变化时携带 `DeviceId`、操作 ID、已见 `AccountStateSeq` 和每流水位。relay 幂等接收并返回新的账户状态序列；设备断线后从账户序列补拉，不以本地游标覆盖服务器状态。

草稿默认是设备私有的，避免多人设备上的同一输入框发生静默覆盖。用户显式开启“同步草稿”后，草稿使用带 `DeviceId` 的版本向量；并发编辑保留两个草稿副本并提示用户选择，**绝不自动丢弃一端内容**。

**`membershipRevision` 只表示群成员关系，不能用作已读状态。** 群成员的阅读位置是账户级 `GroupMessageSeq` 水位，个人设备可另存本地滚动位置但不影响账户未读计数。

## 11. 组织、工作区与角色

**组织是协作资源和权限的顶层所有者，不等同于一个聊天群。**

组织包含多个工作区；工作区包含多个项目；项目可以关联多个群、共享存储目录、公共 Bot、工作项和协作会话。

```text
组织
  └─ 工作区
      └─ 项目
          ├─ 群
          ├─ 共享存储目录
          ├─ 公共 Bot
          ├─ 工作项
          └─ 协作会话
```

账号在组织、工作区和项目三个层级分别获得角色；**较小范围的授权可以增加权限，不能突破上层的显式拒绝或账号冻结**。

### 11.1 角色表

| 角色 | 主要职责 | 默认权限 |
|---|---|---|
| 组织所有者 | 管理组织和最终责任 | 管理组织、管理员、账单、保留策略、全部工作区 |
| 组织管理员 | 日常组织运维 | 管理成员、工作区、邀请码、组织插件、审计和组织级通知 |
| 账单管理员 | 管理订阅与预算 | 查看套餐、发票、预算、成本和购买型模型策略，不管理成员或聊天内容 |
| 分析管理员 | 管理指标规则 | 配置分析保留期、比较组和报告模板；只能在授权范围查看聚合数据 |
| 项目经理 | 管理项目和分派 | 创建项目群、工作项和公共 Bot；分派、调整优先级、查看项目进展与授权项目分析 |
| 开发者 | 执行被授权工作 | 加入项目群、接收工作项、上传和共享项目资源、在权限内调用 Bot |
| 成员 | 参与协作 | 参与获授权群聊、查看和评论获授权资源 |
| 访客 | 外部协作 | 仅访问被单独授予的群、资源或工作项；默认可在被授予的群内发言，但不能下载、再共享、创建群、邀请成员或调用 Bot |
| 审计者 | 只读审查 | 查看获授权的审计记录、版本和元数据，默认不读消息正文 |
| Bot 服务账号 | 自动化执行 | 只拥有其项目策略明确授予的读取、发言和工具权限 |

> **角色只定义默认能力，资源操作仍由资源 ACL 决定。**

例如，项目经理可以把一个项目文件夹或协作会话授予某位开发者；**该开发者不能因此读取项目经理的私聊、其他项目或组织账单**。

### 11.2 组织状态与所有权

组织、工作区、项目和成员关系都具有不可变 ID、版本号、创建者、创建时间、修改时间和状态。

组织状态为 `active`、`suspended` 或 `archived`；挂起组织时停止新写入、Bot 调用和外部通知，但保留只读和恢复能力。

组织所有者可以转让所有权，也可以增设第二名所有者。**转让是高风险动作**：发起方与接收方都需要确认挑战与第二验证因素，接收方**必须**是状态为 `active` 的组织管理员，转让在**同一事务**中改变角色、写入审计并通知全体管理员。

唯一所有者失能、离职或长期不可达时，使用组织恢复路径而不是个人账号恢复：持有离线组织恢复包的部署管理员，或满足阈值的组织恢复守护人，可以发起所有权接管。接管进入默认 7 天的公示期，期间向全体管理员与账单联系人持续通知，原所有者可随时中止；公示期结束后完成转让并写入不可变审计。自建组织在创建时即被要求导出组织恢复包或指定守护人，因此这条路径始终可用。

**组织不允许没有 `active` 所有者。** 所有者注销账号、被移除或被挂起时，若无其他所有者则组织进入 `suspended` 并触发接管流程，**不静默降级为无主状态**。

成员状态为 `invited`、`active`、`suspended`、`removed`；**只有 `active` 成员**能接收新授权、发送项目消息、下载资源或调用 Bot。

所有角色、ACL、组织状态和成员状态的改变都由**显式命令**完成，携带版本号和幂等键；发生并发变更时返回 `VERSION_CONFLICT`，**不能静默覆盖**。

## 12. 组织类型、容量与订阅

组织类型决定运维责任、容量上限和可启用能力，不改变基础权限模型。

| 类型 | 运维方式 | 默认适用规模 | 主要限制 | 额外能力 |
|---|---|---|---|---|
| 自建组织 | 组织自行部署 relay、数据库和对象存储 | 个人、班级、小型私有团队 | 默认 32 人；上限、存储和模型额度由部署管理员配置 | 本地身份、私有网络和自定义保留策略 |
| 团队版 | 平台托管或由组织购买受管服务 | 3 至 100 人团队 | 成员数、活跃项目、共享存储、Bot 调用和模型预算按套餐限制 | 组织插件目录、基础仪表盘、项目报告和标准支持 |
| 企业版 | 合同制受管或私有化部署 | 多工作区、多部门组织 | 限制由合同和部署配置确定，可按部门、区域或成本中心分配 | 单点登录、成员目录同步、审计导出、企业插件审批、细粒度分析和服务等级支持 |

所有限制集中在版本化的 `PlanLimits` 配置中，包括成员数、访客数、工作区数、活跃项目数、群人数、共享存储字节数、单文件大小、保留期、并发 Bot 调用、模型预算和仪表盘保留期。

超过限制时，relay **在创建或提交前**返回明确错误，例如 `MEMBER_LIMIT_REACHED`、`STORAGE_QUOTA_EXCEEDED`、`BOT_BUDGET_EXCEEDED`；**不得先接收资源再异步删除**。

团队版和企业版将订阅账户与组织分离。只有组织所有者或被授予账单权限的管理员能查看套餐、用量、发票、预算和付款方式。

订阅状态为 `trial`、`active`、`past_due`、`grace_period`、`restricted`、`cancelled` 或 `expired`。

进入 `restricted` 后停止新建超额资源、购买型模型调用和新插件安装，但保留数据读取、导出、付款恢复和管理员删除资源的能力；**不得因付款失败直接删除组织数据**。

自建组织没有平台计费状态，但仍可启用管理员定义的配额，避免单个成员、Bot 或项目耗尽服务器资源。

---

# 四、协作能力

本篇定义用户可见的协作功能。贯穿全篇的原则是：**默认不共享，共享必显式，AI 摄入必留痕**。

## 13. 联系人与群聊

联系人请求使用稳定请求 ID，状态为 `pending`、`accepted`、`rejected` 或 `expired`。

拉黑不是联系人状态，而是有向记录 `Block(actorAccountId, subjectAccountId)`；因此双方可以分别拉黑或解除拉黑。

**只有双方联系人关系已接受、且不存在任一方向拉黑记录时，relay 才接收私聊消息。**

联系人请求默认 30 天后转为 `expired`；发送者可重发，但对同一目标的重发受速率限制，且被拉黑时不提示差异。

删除联系人是独立于拉黑的操作：它把关系状态置为 `removed`，双方不再满足私聊接收条件，但不创建 `Block` 记录，因此对方可以再次发起联系人请求。删除联系人不删除已投递的历史消息，也不撤销已授权的附件；本地历史是否保留由用户自行选择。

### 13.1 群与成员版本

relay 拥有每个群不可变的 `GroupId`、归属范围、当前群名、成员集合与单调递增的 `membershipRevision`。

私聊和私有群以已接受联系人为成员来源；项目群以有效项目成员为成员来源，不要求成员两两互为联系人。

项目群创建者必须具备项目群管理权限，群成员的项目权限被撤销或成员被移除时，relay 在同一事务中移除其群成员资格并产生系统事件和通知。

初始版本要求被邀请者接受或拒绝群邀请，接受后才成为有效成员。

群邀请状态为 `pending`、`accepted`、`rejected`、`expired` 或 `withdrawn`，默认 14 天后过期。邀请者可主动撤回未接受的邀请；成员在邀请待处理期间被移出项目或组织时，邀请随同一事务失效。重发邀请创建新的邀请记录并沿用同一去重键，不产生重复收件箱条目。

初始版本中只有群主或项目群管理员可以邀请成员、移除成员、改群名和转让群主。

每次群操作都在同一 relay 事务中追加不可变群事件，并递增该群的 `membershipRevision`。

客户端同步 `{ GroupId: membershipRevision }` 映射，不能使用单个全局花名册版本。

联系人详情和群成员列表显示 relay 聚合后的在线状态与最近可用状态；该显示只用于选择联系对象和理解投递提示，不能替代已读回执。

### 13.2 群历史授权

新成员默认只获得群定义和邀请之后的群事件，但 P1 必须提供 `GroupHistoryGrant`，避免项目协作出现历史真空。

群主或项目群管理员在邀请流程中可选择“不共享历史”“共享最近 N 天”“共享从指定消息游标开始的历史”或“共享已标记为项目背景的历史”。授权范围还独立选择消息、附件、资源引用、工作项和仓库更新。

relay 在**一个事务**中冻结历史上限游标、创建接收人的历史读取授权、为允许的附件/资源创建受限读取授权，并写入群事件、审计和通知。**新成员只读取该不可变范围，不能因后来获得群成员资格而自动扩大到更早历史。**

历史授权带有效期，默认与组织 `RetentionPolicy` 中的消息附件授权一致（自建 30 天、团队版 90 天、企业版按策略），到期后不再可读取新内容。授权可由群主或项目群管理员续期，续期是显式命令并产生审计与通知；续期不扩大原有范围上限游标。绑定项目背景的长期授权可配置为随项目状态存续，项目归档时一并停止。

管理员可撤销未下载或未打开的历史授权；**已下载内容仍受设备本地副本限制，界面必须提示不能远程抹除**。端到端加密群的历史授权还需要相应 epoch 的密钥包；无法安全提供密钥包时，界面只允许共享项目资源和摘要，**不能虚假承诺可回溯消息正文**。

### 13.3 仓库绑定

项目群可以选择性绑定一个或多个仓库。绑定记录 `RepositoryBinding` 属于项目和群，包含仓库提供方、规范 URL、默认分支、显示名、访问模式、绑定者、版本和状态。

访问模式为 `link_only`、`read_sync` 或 `write_automation`。`link_only` 只在群资料页显示可点击仓库链接，不保存仓库 token；`read_sync` 允许 relay 验证提交和接收只读 webhook；`write_automation` 只在企业策略和独立审批通过后允许 Bot 或工作流创建分支、提交或合并请求。

绑定仓库需要项目群管理员权限。relay 对规范 URL 去重，校验仓库是否属于允许的提供方和组织范围，并将绑定事件、群系统消息和项目通知写入同一事务。

成员点击仓库链接时直接跳转到仓库提供方；**dsh-chat 不把成员个人的 Git 凭证代理给其他成员**。

仓库绑定状态为 `pending_verification`、`active`、`degraded`、`revoked` 或 `removed`。验证失败、webhook 签名不符或远端不可访问时进入 `degraded` 并通知群管理员，但不影响普通聊天和本地开发。

## 14. 消息模型

本地创建的每条消息都带有 UUIDv7 `MessageId`、不可变发送者账号、目标、类型、创建时间和客户端幂等键。

relay 将 `(senderAccountId, MessageId)` 作为幂等键，重试时返回首次接收结果。

relay 为私聊收件人队列项分配独立且单调递增的 `DeliverySeq`；群消息写入按 `GroupId + membershipRevision` 分区的 `GroupLog`，并获得单调递增的 `GroupMessageSeq`。

**relay 不以发送方时钟排列会话**；客户端以 relay 接收时间和稳定的并列排序键展示消息。

P0 支持的消息类型：

```text
text { body }
```

P1 支持的消息类型：

```text
image { attachmentId, caption? }
file  { attachmentId, filename, mime, size, caption? }
quote { targetMessageId, senderLabel, excerpt, targetType }
system { action, renderedText }
```

引用是附在新消息上的非递归快照。

接收方即使没有原消息也能渲染引用；只有原消息在本地时，点击引用才可以跳转到原消息。

> **所有展示文本都通过安全的 DOM 文本 API 渲染，任何消息字段都不得作为 HTML 插入。**

### 14.1 消息编辑与撤回

**消息正文不是可原地覆盖的字段。** 每个 `MessageId` 具有单调递增的 `MessageRevision`，初始正文为 revision 1；编辑追加不可变 `message_edited` 事件，撤回追加 `message_revoked` tombstone 事件。

编辑事件包含 `MessageId`、目标 revision、编辑者、编辑时间、策略版本和新内容摘要；只有原发送者在组织配置的编辑窗口内，且消息未被撤回、未触发保留冻结时可以编辑。接收方和 host 只接受比本地 revision 更高的事件，因此重复投递或乱序同步不会把新正文覆盖为旧正文。

撤回由原发送者或具备合规权限的管理员发起。私聊撤回写入每位已投递收件人的控制事件流；群撤回以新的 `GroupLog` tombstone 事件追加。在线和离线设备在下次同步时都收到该事件，本地把正文替换为撤回占位并保留最小审计元数据。

> **撤回不承诺擦除已被用户截图、导出、复制、端到端设备缓存或通过历史授权下载的内容。** 界面和审计记录**必须**明确“已从受控视图撤回”的边界。

引用和历史授权引用的是特定 `MessageRevision`。原消息编辑或撤回后，已发送引用仍展示发送时快照，并标记原消息当前已编辑或已撤回，**避免静默篡改历史上下文**。

## 15. 消息交互功能范围

以下功能被显式定级，避免逐项遗漏或在实现期临时加入而绕过权限与同步语义。任何被标为延后的功能，其入口在此前显示为未安装或返回 `NOT_IMPLEMENTED`。

| 功能 | 定级 | 说明 |
|---|---|---|
| 表情回应 | P1 最小模型 | 见 15.1 |
| 富文本与代码块 | P1 | 受限 Markdown 子集：粗体、斜体、行内代码、围栏代码块、链接、有序/无序列表；服务端按 schema 校验并存储结构化节点，客户端安全 DOM 渲染，不接受原始 HTML |
| 消息转发 | P2 | 见 15.2 |
| 消息线程 | P2 | 见 15.3 |
| 群公告与置顶 | P2 | 群级单条置顶与公告，属群事件，权限同改群名 |
| 群资料（头像、描述） | P1 | 头像走 `BlobObject` 与资源扫描链路，不单开上传通道 |
| 只读群 | P2 | 以群策略表达，不新增群类型 |
| 语音消息 | 延后 | 若启用，作为文件子类型处理，遵循 P2 视频的同类限制；不实现独立流媒体协议 |
| 消息定时发送、免打扰时段 | 延后 | 与通知偏好合并设计，不单独建模 |
| 已读回执（逐人可见） | 不做 | 与在线状态边界一致；账户级已读水位只用于未读计数，不向他人暴露 |
| 输入中提示 | 不做 | 属于键盘活动推断，违反在线状态边界声明 |

### 15.1 表情回应

回应绑定 `MessageId` 与具体 `MessageRevision`，记录 `(MessageId, revision, accountId, emoji)` 四元组并以其为幂等键，同一账号对同一表情重复提交为幂等。回应是独立于消息正文的轻量事件，不产生新的 `GroupMessageSeq`，通过账户状态流跨设备同步。可回应者限于该消息的有效读取者；消息被撤回后回应一并隐藏。表情集合由组织策略限定为固定白名单，不接受任意字符串或自定义图片，避免成为旁路内容通道。回应默认不产生通知。

### 15.2 转发、共享与引用的区别

三者语义不同，必须在界面上可区分：

| 操作 | 产生什么 | 权限判定 | 接收方看到 |
|---|---|---|---|
| 引用 | 附在新消息上的非递归快照 | 发送者当前对目标会话的发言权 | 发送时的正文摘录，标记原消息已编辑/已撤回 |
| 转发 | 新消息 + 不可变摘录快照 + 附件的新 `MessageContentGrant` | 逐条复检原消息读取权与附件授权 | 独立消息，标注来源类型与转发者，不含原会话上下文 |
| 共享 | `ShareableItem` 与资源 ACL 授权 | 完整共享闭环与确认 | 受控对象，可撤销、可设有效期 |

**转发不是复制物理字节**：附件复用同一 `BlobObject`，只创建接收人范围的新授权。**转发跨组织一律禁止。** 原消息所属目录禁止快照时，转发返回 `RESOURCE_SNAPSHOT_FORBIDDEN`。转发链不递归展开，只标注直接来源。

### 15.3 线程

线程是挂在某条根消息上的子序列，拥有 `ThreadId`、根 `MessageId` 与独立单调的 `ThreadMessageSeq`，但仍存储在所属群的 `GroupLog` 分区内，**不新建权限域**。**线程权限完全继承群与历史授权，不能通过线程扩大或缩小可见范围。**线程回复默认只通知线程参与者与被 @ 的成员，不打扰全群；根消息被撤回时线程转为只读并保留已发生的回复。历史授权按根消息游标整体授予或不授予线程，不做部分裁剪。

## 16. 附件与内容授权

附件和组织资源共用内容寻址的 `BlobObject` 与两阶段上传协议；消息附件、资源版本和历史授权只创建不同的元数据与访问授权，**不复制物理字节**。

1. host 创建本地附件记录，并在任何 relay 请求前保存原始字节。
2. host 请求 relay 创建上传预约；预约绑定 `MessageId`、预期目标、MIME 类型、字节长度、摘要和过期时间。
3. host 使用该预约上传字节。
4. host 发送引用该预约附件的消息。
5. 只有预约、摘要、发送者和目标全部匹配时，relay 才接收消息；同一事务把 `BlobObject` 绑定到已接收消息，并创建接收人范围的 `MessageContentGrant`。

把组织资源发送到群时，用户必须选择“发送实时资源引用”或“发送版本快照”。实时引用仅在所有目标成员已有该资源 ACL 时可用；版本快照创建一个绑定资源版本、消息、收件人范围和保留期的 `MessageContentGrant`，使接收人即使不具备目录 ACL 也可在授权期内打开该特定版本。

目录策略可以禁止版本快照，例如受限人事、财务或密钥目录。此时 relay 在发送前返回 `RESOURCE_SNAPSHOT_FORBIDDEN`，而不是发送一个接收人无法打开的死链接。

> **relay 根据已接收消息的发送者、收件人和 `MessageContentGrant` 授权下载附件，而不能只依据当前群成员关系或共享目录 ACL。**

该规则允许**退群成员在保留期内重新下载自己已获授权的媒体**，也**阻止后来入群的人读取旧附件**。

relay 必须将 `BlobObject` 保留至所有有效的消息/资源引用和 `MessageContentGrant` 到期后，再额外保留一个可配置的恢复窗口；同一 blob 被资源库引用时不能因某条消息 ACK 而删除。

附件下载失败时，消息仍然持久化，状态为 `attachmentState = pending`；host 反复重试至保留期结束，并显示“附件不可用”的终态，**而不能假装图片已经送达**。

P1 接受 PNG、JPEG、WebP、GIF 和通用文件，应用 MIME 白名单、适用时的魔数验证、可配置的大小上限、摘要验证、文件名净化和按账号/群划分的配额。

P2 中视频是文件的一个子类型，不是独立流媒体协议；启用前必须另行定义更严格的大小、时长、转码、预览、带宽和保留期限制。

浏览器以二进制 multipart 或流式请求体把选定文件交给 host；不得把数 MB 的数据扩展成 JSON base64。

host 对文本、图片、文件、说明、引用、表情回应和未来命令统一执行输入策略：最大长度、允许类型、速率限制、目标授权、幂等键和审计记录。

## 17. 工作项与通知

**工作项把“需要谁继续做什么”从聊天消息中分离出来。**

工作项属于一个项目，包含标题、描述、优先级、负责人、协作者、关联群、关联资源、截止时间和当前版本。

工作项状态为 `draft`、`open`、`assigned`、`in_progress`、`blocked`、`in_review`、`done`、`cancelled` 或 `archived`。

工作项分派另有**独立的签收状态** `offered`、`acknowledged`、`declined`、`expired` 或 `reassigned`。**它不复用聊天消息或通知的 `read` 状态。**

负责人通过明确“接受任务”“拒绝并说明原因”或“请求调整”命令改变签收状态；项目经理可以设置签收期限和逾期提醒。通知已送达或已阅读只表示收件箱状态，不能代表任务已知晓、同意或开始执行。

只有项目经理、项目管理员或被授予分派权限的成员可以变更负责人、优先级和终态；开发者可以认领已开放的工作项、报告阻塞、提交产物和请求评审。

状态变更必须使用命令而不是直接修改字段，并记录操作者、理由、前一版本、后一版本和关联产物。

工作项之间可以建立依赖关系 `blocks` 与 `depends_on`，以及父子分解关系。依赖是同项目内的显式引用，创建时校验不产生环；跨项目依赖需要双方项目的分派权限并各自留审计。父工作项不自动因子项完成而完成，但在存在未完成子项时进入终态需要显式确认理由，避免静默丢失未完成工作。

截止时间是项目本地时区下的时间点，存储为 UTC 并显示时区标识。逾期不自动改变工作项状态，只产生逾期提醒并在分析中标记；系统不因逾期自动重新分派或降低优先级，这类判断留给项目经理。

### 17.1 通知与收件箱

通知是持久化收件箱记录，不是 SSE 推送本身。

每个应通知的领域事件先在数据库事务中写入 `Notification`，再由 outbox 任务向在线 host 推送事件。

通知含接收人、事件类型、资源引用、来源操作者、最小可展示摘要、优先级、创建时间、去重键和状态。

通知状态为 `queued`、`delivered`、`seen`、`read`、`dismissed`、`expired` 或 `failed`。

浏览器 SSE 断开、桌面通知权限被拒绝或设备离线都不会让通知丢失；host 重连后从收件箱游标补拉。

通知发送失败仅影响即时提醒，不影响消息、工作项或资源的已提交状态。

默认通知事件包括联系人请求、群邀请、在线状态订阅变化、@ 提及、工作项分派或变更、评审请求、共享资源、协作会话邀请、仓库绑定或提交更新、Bot 响应、上传失败和权限变更。

去重键与聚合是两件事。去重键防止同一领域事件重复投递产生多条记录；聚合把多个不同事件折叠为一条可展开的收件箱条目。

聚合按 `(接收人, 来源对象, 事件类型)` 在可配置的聚合窗口（默认 5 分钟）内进行：同群的多次 @ 提及折叠为一条“N 条新提及”，保留最早与最新事件引用和计数，不生成 N 条独立记录；同一工作项的连续状态变更折叠为最新状态并保留变更次数。聚合条目的已读语义作用于整条，展开后逐条跳转。

安全类通知、工作项签收请求、权限变更和风险处置不参与聚合，始终逐条呈现。聚合只影响收件箱呈现与推送节流，不影响审计事件的逐条完整性。

用户可按组织、项目、群、事件类型和优先级设置静默、仅收件箱、页面角标或桌面通知；项目管理员不能绕过成员的个人静默设置，除非组织明确配置紧急事件策略。

已验证邮箱只接收设备注册、异地登录、密码/认证方式变更、设备撤销和组织安全管制等事务性安全邮件。此类邮件不能被普通项目静默规则关闭，但用户可以在安全设置中更换邮箱或启用更强验证方式。

## 18. 评审与评论

评审是工作项从 `in_review` 进入 `done` 的显式关口，不是聊天消息里的口头确认。

评审请求包含 `ReviewId`、目标工作项、请求者、指定评审人、关联产物版本或 `CodeChangeRecord`、请求说明和截止时间。评审状态为 `requested`、`in_progress`、`approved`、`changes_requested`、`declined`、`expired` 或 `superseded`。

评审人必须是有该项目读取权限的成员，且默认不能是提交者本人；组织策略可要求最少评审人数或指定角色，企业版可要求双人评审。指定评审人产生签收类通知，其送达或已读不代表评审已开始。

`approved` 只对评审时锁定的产物版本生效。关联产物或提交在评审后发生变化时，评审自动转为 `superseded` 并要求重新评审，不允许“批准一个版本、合入另一个版本”。

只有获得组织或项目策略要求的评审结论后，工作项才能由有权成员转入 `done`；策略未要求评审的项目可直接完成，但该差异必须记录在项目配置并写入分析口径，避免跨项目排行时把“无需评审”误算成高效率。

评论属于工作项、资源版本、评审或协作会话，不属于群消息，因此不进入 `GroupLog`，也不参与消息撤回语义。评论包含作者、目标对象与版本、正文、创建时间和 `CommentRevision`；编辑追加修订，删除写入 tombstone 并保留作者与时间。

拥有 `comment` 访问级别的成员可以阅读目标对象并发表评论，但不能修改对象本身、不能改变工作项状态、不能发起或批准评审，也不能再共享。评论中的 @ 提及只能指向对该对象已有读取权限的成员；提及无权限成员时返回 `FORBIDDEN`，不静默丢弃提及也不因此授予访问权。

评论正文与消息正文遵循同一输入策略与安全渲染规则，并同样作为不可信内容处理。

## 19. 共享存储与选择性共享

### 19.1 组织共享存储

组织共享存储不是成员本地媒体目录的集合，而是 relay 管理的版本化资源库。

资源库按组织、工作区和项目建立根目录；每个目录和资源都有独立 ACL、配额、保留策略和版本历史。

资源类型包括文件、图片、视频、文档、代码补丁、压缩包、DSH 生成产物和外部引用。

上传流程使用与消息附件相同的预约、摘要校验和分块上传机制，但资源在上传完成后先处于 `staged` 状态。

上传者提交元数据和目标目录后，relay 创建 `ResourceVersion`；内容校验、病毒扫描或组织要求的人工审批通过后，版本才进入 `available`。

资源版本状态为 `staged`、`scanning`、`approval_pending`、`available`、`rejected`、`quarantined`、`deleted` 或 `expired`。

下载请求总是指向具体 `ResourceVersion`，relay 在每次下载时检查调用者成员状态、目录 ACL、资源 ACL、版本状态和有效期。

同名覆盖不会覆盖旧版本，而是创建新版本；当前版本切换需要目录编辑权限，并产生通知和审计事件。

删除使用软删除和保留窗口。保留窗口结束前，组织管理员可以恢复；物理清理后返回 `RESOURCE_GONE`。

资源库、消息附件、工作项、共享对象、协作会话和 Bot 通过 `ResourceVersion` 或 `BlobObject` 引用同一物理内容。消息侧只保存引用与 `MessageContentGrant`，不能把文件内容复制进聊天数据库，也不能绕过目录禁止快照的策略。

### 19.2 选择性共享

可共享对象统一称为 `ShareableItem`，包括消息选段、附件、图片、文件、DSH 生成产物、工作项、项目说明、协作会话和版本快照。

共享者必须在发送前明确选择对象、目标组织/工作区/项目/群/成员、访问级别和有效期。

访问级别为 `view`、`comment`、`edit` 或 `continue`；被共享者不能再共享，除非共享者或资源策略显式授予 `reshare`。

对聊天消息的共享默认创建不可变摘录快照，而不是授权接收方浏览整个原始私聊。

对文件的共享创建版本化资源引用；接收方只能下载被授予的版本，后续版本必须再次共享或继承明确的目录权限。

对 AI 生成产物的共享必须显示产物来源、模型/工具标记、生成时间、人工编辑状态和已选上下文范围；它不能伪装成某个成员手写的结论。

`ShareableItem` 状态为 `draft`、`pending_confirmation`、`published`、`revoked`、`expired` 或 `deleted`。

共享操作遵循以下闭环：

1. 浏览器创建本地草稿，host 验证资源权限和目标成员状态。
2. 用户确认摘要、目标和访问级别后，host 向 relay 发送带幂等键的发布命令。
3. relay 在一个事务中写入共享记录、资源 ACL、审计事件和接收人通知。
4. relay 返回 `published` 后，发送方界面才显示“已共享”；超时则查询同一幂等键，不能盲目重复授权。
5. 接收人打开通知时，relay 在读取资源前再次检查组织成员、资源 ACL、有效期和资源状态。

权限不足返回 `FORBIDDEN`，目标已离开组织返回 `RECIPIENT_INACTIVE`，资源版本冲突返回 `VERSION_CONFLICT`，过期或撤销资源返回 `SHARE_UNAVAILABLE`。

撤销共享只能阻止后续读取，不能删除已经下载的本地副本或已经被复制到其他已授权产物中的内容；界面必须明确这个限制。

## 20. 协作会话与开发交接

个人 DSH 会话默认是私有的，不能因为加入组织、群或项目而被其他人继续、搜索或提供给 Bot。

用户需要交接开发工作时，从个人会话显式选择一段对话、相关文件、终端输出和已生成产物，创建一个 `CollaborationSession`。

创建者必须确认要公开的上下文范围，并为每个输入生成不可变快照；没有被选中的私人上下文不进入协作会话。

`CollaborationSession` 属于一个项目，带有成员 ACL、目标、当前工作分支或资源版本、关联工作项和独立的 DSH 会话 ID。

协作会话状态为 `draft`、`pending_review`、`active`、`paused`、`completed`、`revoked` 或 `archived`。

拥有 `continue` 权限的成员可以创建一次协作执行尝试，而不是接管创建者的私人会话。

执行尝试状态为 `queued`、`running`、`waiting_for_input`、`succeeded`、`failed`、`cancelled` 或 `superseded`。

### 20.1 沙箱执行约束

执行尝试只能由 `ExecutionProvider` 在独立沙箱中运行，不能在 relay 主进程、成员 host 主进程或 Bot host 主进程内直接执行命令。P2 开始前必须先实现并验收该 provider。

每次尝试创建一个临时工作区，只读挂载经过授权的仓库提交或资源版本；可写输出只能写入临时目录和明确的结果目录。尝试默认没有网络、没有宿主文件系统访问、没有环境变量继承、没有 Docker socket、没有组织秘密和没有用户私人 DSH 会话。

网络、仓库写入、特定命令、包缓存和秘密访问采用逐项能力授权。秘密由短期秘密 broker 按尝试、目标仓库、分支和时限签发，不能写入日志、产物、环境转储或后续尝试。

每个沙箱设定 CPU、内存、磁盘、进程数、网络字节、执行时长、日志字节和并发数配额；超过任何配额时运行时终止尝试，状态为 `failed` 且错误码为 `SANDBOX_QUOTA_EXCEEDED`，保留安全检查点但不自动发布产物。

拉取仓库、构建、测试和 AI 工具调用都在尝试预算内计费。用户或管理员取消、设备受控、项目权限收回、仓库绑定撤销或租约到期时，provider 终止进程树、回收临时目录和短期秘密。

尝试创建提交、分支或合并请求前必须显示变更摘要并获得拥有写仓库权限的成员确认；自动化 Bot 只能使用独立服务账号和受限分支，不能把写权限借给个人设备。

同一工作项允许多人讨论，但同一可写资源版本只允许一个持有租约的执行尝试修改；其他尝试进入只读、等待或创建独立分支，提交时使用版本比较避免覆盖。

### 20.2 候选产物与接受流程

成功的交接会把结果作为新的产物版本或合并请求写入项目，并通知创建者、负责人和订阅者。

执行成功不等于产物被接受。尝试产出先进入 `pending_acceptance` 的候选产物，携带来源尝试 ID、输入快照引用、变更摘要、资源消耗和 AI 使用引用；**候选产物默认只对协作会话成员可见，不自动成为资源库当前版本**。

接受候选产物需要拥有目标资源写权限的成员**显式确认**，确认时展示与当前版本的差异摘要和版本比较结果。版本在尝试期间被他人推进时返回 `VERSION_CONFLICT`，**接受者只能基于最新版本重新比较或另建分支，不能覆盖**。

拒绝候选产物需要记录理由，产物转为 `rejected` 并保留可追溯记录；候选产物按 Bot/协作执行日志的保留期清理。

失败、取消或租约过期会保留执行日志、最后安全检查点和失败原因；不会自动把半成品写入共享资源，也不会悄悄归属给原创建者。

协作会话的执行日志、候选产物和输入快照与其来源的个人会话解耦：原创建者撤销协作会话后，已接受的产物版本仍归属项目并保留归因，未接受的候选产物停止可见并按保留期清理。撤销不追溯删除接手者已合法获得的产物，界面必须说明这一边界。

## 21. 搜索与索引

搜索是被权限约束的查询能力，**不是对全部历史的自由文本扫描**。任何搜索结果都必须能被独立的授权检查复算；**索引命中不构成读取授权**。

可搜索范围按对象类型分别定义，不共用一个全局索引：

| 对象 | 索引源 | 可搜字段 | 范围约束 |
|---|---|---|---|
| 群消息 | `GroupLog` 已提交事件 | 正文、发送者、时间、附件文件名 | 只索引调用者拥有历史授权的区间 |
| 私聊消息 | 本地已持久化投递记录 | 正文、时间、附件文件名 | 仅 host 本地，不进 relay 索引 |
| 资源版本 | `ResourceVersion` 元数据 | 文件名、标题、标签、目录路径 | 默认不索引文件正文 |
| 工作项 | 工作项字段 | 标题、描述、负责人、状态 | 按项目 ACL |
| 代码更新 | 已 `verified` 的 `CodeChangeRecord` | commit 摘要、分支、关联工作项 | 不索引代码正文与私有 diff |
| 插件目录 | 已 `published` 的发布记录 | 包名、用途、发布者 | 按组织目录可见性 |

索引管线以领域事件为唯一输入，与 outbox 共用投递语义：`message_accepted`、`message_edited`、`message_revoked`、`resource_available`、`resource_quarantined`、`code_update_verified` 和工作项状态事件驱动增量更新。索引任务是可重放的异步任务，具有与其他异步任务相同的 `queued` 至 `dead_letter` 状态；索引失败只影响可发现性，不影响消息、资源或工作项的已提交状态。

索引条目状态为 `pending`、`indexed`、`stale`、`suppressed` 或 `purged`。`message_revoked`、`resource_quarantined`、历史授权撤销、成员移除和保留期到期都必须产生 `suppressed` 或 `purged` 转换；撤回的消息正文不得在索引中残留。索引删除与 `RetentionPolicy` 同批次执行，不单独设定更长的保留期。

查询执行顺序固定：解析 `OrganizationId` 与成员状态，计算调用者可见的索引分区，执行受限查询，然后对每条候选结果**逐条**复检资源 ACL、历史授权区间、对象状态和有效期，最后才返回。**复检失败的条目静默丢弃，不返回占位符，也不在总数中计入，避免通过结果计数推断不可见内容的存在**。

搜索请求受独立的速率、结果条数、分页深度和查询复杂度上限约束；不支持可导致全库扫描的前缀通配、正则或空查询。查询词、结果 ID 和调用者写入审计，但查询词不写入普通日志。

索引按 `OrganizationId + policyRevision` 分区。切换组织时 host 关闭前一组织的索引句柄；**跨组织搜索在任何层级都不可用，包括管理员与分析角色**。

端到端加密模式下服务端搜索的降级遵循 `OrganizationEncryptionPolicy` 功能矩阵：

| 模式 | 服务端索引 | 客户端本地索引 | 说明 |
|---|---|---|---|
| `server_managed` | 全部可搜对象 | 可选加速 | relay 按权限读取正文构建索引 |
| `e2ee_without_bot` | 仅元数据（文件名、标题、工作项、提交摘要） | 消息正文仅在成员设备本地索引 | relay 强制禁用正文索引 |
| `e2ee_with_bot_member` | 同上 | 同上 | Bot 设备的解密能力不用于构建服务端索引 |

设备侧本地索引只覆盖该设备已同步并解密的区间，因此不同设备的搜索结果可能不同；**界面必须显示已索引区间和“更早历史需在其他设备或补拉后可搜”，不得让用户误以为结果是全量的**。本地索引与解密材料同等保护，设备进入 `restricted` 时随敏感缓存一并清除。

P0 只提供 host 本地的私聊与联系人查找，relay 不提供搜索 API，入口显示为未安装。P1 加入群消息与资源元数据的服务端索引。P3 加入工作项、代码更新与插件目录搜索。P4 按上表执行 E2EE 降级，并在切换加密模式前把索引能力变化写入管理员确认的降级清单。

## 22. 仓库与 AI 辅助开发记录

`CodeChangeRecord` 将一个已验证的 Git 提交与项目、仓库、作者、工作项、协作会话和 AI 辅助来源关联起来。

记录包含 `RepositoryBindingId`、完整 commit hash、父 hash、分支或合并请求、外部作者标识、映射后的组织账号、提交时间、变更统计、关联工作项、关联协作会话、AI 使用引用和验证来源。

AI 使用引用只表明该提交在明确的 DSH/公共插件/Bot 调用后产生，或用户在提交前主动关联了这些调用；**它不能证明每一行代码都由 AI 生成，也不能替代代码评审**。

host 可以在用户提交代码后创建 `reported` 记录，但只有仓库提供方 webhook、受限 API 查询或可信镜像验证了 hash、仓库和作者映射后，记录才变为 `verified`。

记录状态为 `reported`、`verifying`、`verified`、`rejected`、`orphaned` 或 `superseded`。

`rejected` 表示 hash 不存在、仓库不匹配、webhook 签名无效或作者映射冲突；`orphaned` 表示提交存在但无法安全映射到组织成员或项目；两者都不进入成员贡献、周报或排行榜。

一次提交可关联多个 AI 调用和工作项，一个 AI 调用也可以关联多个提交。**关联必须由用户或项目策略明确发起，不能通过提交信息中猜测关键词自动建立。**

relay 以 `(RepositoryBindingId, commitHash)` 去重。远端强制推送、分支删除或合并重写不会删除历史记录，而是新增更新事件并把受影响记录标记为 `superseded` 或保留其最后验证状态。

项目群展示最近的已验证提交、关联工作项、作者、评审/合并状态和 AI 辅助标记；点击 hash 跳转到仓库提供方。默认不在群内展示代码正文、私有 diff 或未验证的提交摘要。

Bot 或工作流提交代码时**必须**使用独立服务账号和独立分支，记录 `authorKind = bot`；**系统不得把该 hash 计入任何人类成员的开发统计或排行**。

## 23. 群内公共 Bot

群 Bot 是**独立配置的 DSH 智能体身份，不是任一成员私人 DSH 会话的共享副本**。

创建或启用群 Bot 前，relay 必须检查 `OrganizationEncryptionPolicy`。`e2ee_without_bot` 模式下 Bot 入口、调用 API、自动触发器和相关插件目录项全部不可用；`e2ee_with_bot_member` 模式下只允许已被成员确认的 Bot 设备参与相应群 epoch。

### 23.1 权限静态绑定

> **Bot 的权限静态绑定在其 `BotServiceAccount`、项目 ACL、能力清单和预算上，不随调用成员的角色动态提升。** 调用者只能证明“有权请求这个 Bot”，**不能把自己的文件、协作会话、秘密或写仓库权限借给 Bot**。

Bot 读取资源时使用 `botCapability ∩ botResourceAcl ∩ invocationPolicy` 的交集。**人类共享给另一名人类的 `CollaborationSession` 默认不授予任何 Bot**；需要 Bot 读取时，资源所有者**必须**单独创建带范围、时限和只读权限的 `BotAccessGrant`。

若组织确实需要委托，使用**不可转授权、最小范围、短时效**的 `DelegationGrant`，并同时满足调用者和 Bot 的静态权限；**它不能包含私人会话、设备私钥、长期仓库 token 或组织管理能力**。

### 23.2 调用与上下文冻结

群主通过明确群命令添加 Bot，并选择其模型或 preset、允许工具、最大上下文、数据保留期、速率限制和可调用成员。

relay 只有在验证成员身份和 Bot 策略后，才会把 Bot 调用路由给 Bot host。

Bot host 为每个 `(GroupId, BotId)` 创建或恢复一个隔离的 DSH 会话。

该会话的上下文只包含 Bot 策略明确允许的群消息、Bot 响应和配置的系统指令；**它绝不读取成员个人 DSH 会话或私聊内容**。

Bot 响应是带有 `authorKind = bot` 和 Bot 身份标识的普通群消息，并且永远按不可信纯文本或受限附件渲染。

> **只有用户在客户端显式提交的交互意图才会进入命令解析器。群消息、Bot 输出、AI 输出、仓库 webhook、插件输出和引用文本绝不被二次解析为斜杠命令、工具调用、确认动作或权限请求。**

Bot 只可以响应显式 @、由用户界面生成的命令调用或已启用的触发策略发言；每条自动消息都带调用引用，并受速率和工具权限约束。

移除 Bot 后停止后续处理，并按照群保留策略删除或归档该群 Bot 会话。

公共 Bot 的共享上下文采用“项目记忆”而不是无限群记录：策略定义可读取的群、项目资源目录、工作项状态、最大消息数、最大字符数和保留时间。

每次调用先**冻结本次允许的上下文清单**，再向 Bot host 发送；**调用期间的新消息和后续权限变化不自动混入同一请求**。

Bot 可以把可复用的总结、计划、代码建议或文件产物发布到项目资源库，但发布前必须经过项目策略规定的人工确认，或者被标记为 `bot_generated` 且限制为草稿可见。

Bot 调用状态为 `requested`、`authorized`、`queued`、`running`、`awaiting_confirmation`、`published`、`rejected`、`failed`、`cancelled` 或 `expired`。

Bot host 超时、模型失败、工具拒绝和策略违反都进入 `failed`，保留不含秘密的失败摘要并通知调用者；这些失败不会改变工作项状态、覆盖项目资源或以成员名义发言。

## 24. 公共插件与工具目录

组织公共插件是管理员审核后供成员一键启用的 DSH 插件、工具、工作流或 Bot 模板。

公共插件属于组织或工作区，不属于某位成员的个人 profile；成员点击启用后，插件安装到本人的 DSH profile，但其可访问的数据和服务仍受组织策略与资源 ACL 限制。

插件发布记录至少包含包名、精确版本、发布者、来源、签名或摘要、用途、支持平台、配置 schema、所需权限、数据流说明、变更记录和回滚版本。

插件权限以能力声明表达，包括读取指定项目资源、写入指定项目资源、发送群消息、调用模型、使用外部网络、读取组织秘密、运行工作流和代表 Bot 发言。

组织管理员**不能以“已审核”为理由让插件绕过成员、项目或资源权限**；每次敏感调用仍在运行时校验调用者和目标资源。

插件发布状态为 `draft`、`submitted`、`reviewing`、`approved`、`published`、`deprecated`、`revoked` 或 `rejected`。

发布链路如下：

1. 发布者提交精确包版本、权限清单、测试证据和数据流说明。
2. 审核者检查来源、签名、依赖、权限、网络目标、秘密读取范围和回滚方案。
3. 审核通过后，relay 写入不可变发布版本和组织目录事件；成员收到“可用工具”通知。
4. 成员一键启用时，host 下载已批准的精确版本并再次展示所需权限；安装失败只影响该成员，不撤销已发布版本。
5. 管理员撤销版本后，relay 在同一事务中将版本设为 `revoked`、递增插件策略修订、使启用令牌和能力租约失效、写入撤销 outbox 与审计。调度器立即停止排队任务；运行中的插件/Bot/工作流收到带截止时间的取消命令，截止前未退出则由其执行 provider 强制终止进程或隔离实例，并回收网络、秘密和写入租约。任何迟到结果都因版本或能力租约已失效而被 relay 拒绝。host 收到失效事件后立即禁用版本，不能等待下一次常规同步。

插件升级**不是静默替换**。新版本必须重新审核，成员或管理员按组织策略选择立即升级、分批升级或保持旧版本至截止日期。

企业版可配置审批人、双人审批、允许来源、软件物料清单、漏洞扫描、签名策略和分阶段发布；自建组织至少要保留发布者、版本、权限和撤销记录。

## 25. 仪表盘、报告与排行

组织仪表盘服务于容量、成本、协作流转和交付质量管理，**不把聊天内容、私人会话或原始提示词当作统计数据**。

组织所有者和具备分析权限的管理员可以查看组织级 token、模型成本、活跃成员、项目流转、Bot 调用、共享存储、插件使用和已验证代码更新的聚合数据。

项目经理只能查看自己有权限的项目及其成员汇总；普通成员默认只能查看自己的数据和项目的匿名分布。

原始用量事件由 host、relay、Bot host 和经批准的插件产生，至少包含组织/项目/工作项引用、账号或服务账号、角色族、事件时间、模型、输入 token、输出 token、缓存 token、计费金额、调用耗时、执行尝试和匿名化关联 ID。

已验证的 `CodeChangeRecord` 是独立分析来源，提供提交、评审和工作项流转事实；它不上传仓库代码正文，也不把代码行数直接转换为个人得分。

**AI 辅助编码指标不直接把“模型生成代码行数”当作贡献或排行依据。**

系统可记录 AI 生成建议是否被接受、关联的代码提交/评审、工作项复杂度、返工次数、评审结果和执行耗时；**这些都是可审计的辅助信号，不是自动绩效结论**。

分析事件状态为 `collected`、`validated`、`attributed`、`aggregated`、`published`、`corrected` 或 `suppressed`。

无项目归属、缺少授权、来源不可信或不满足最小样本量的事件不能进入个人排行，只能进入经脱敏的运维汇总或被 `suppressed`。

### 25.1 token 与成本大屏

组织大屏显示可配置时间窗内的输入/输出/缓存 token、模型成本、预算消耗、按项目/模型/公共插件/Bot 的分布、超额预测和异常峰值。

默认只显示聚合趋势；查看个人明细需要组织策略明确允许，且查询会留下审计记录。

预算告警按照组织、工作区、项目、Bot 和插件分别设置阈值。告警创建持久通知，可选择阻断新的付费调用、降级到指定模型或等待管理员批准；不能中断已经提交且有安全检查点的协作执行。

### 25.2 个人工作台与个人排行

个人设置页提供“我的工作台”，展示当前组织和可切换组织、绑定设备与登录方式、在线状态可见范围、个人 token/模型成本、预算归属、工作项、最近验证提交、AI 辅助记录、通知和个人周报入口。

个人 token 数据按组织、项目、模型、插件、Bot 和时间窗拆分，显示输入/输出/缓存 token、成本、预算限额和数据来源。**成员只能查看自己的逐项用量，不能借由项目统计反推其他成员的私人调用。**

个人排行页只显示用户在有资格的比较组中的当前分位、分数构成、样本量、计算策略版本和可更正数据；**没有合格比较组时明确显示“不参与排行”，而不是显示零分或空名次**。

### 25.3 AI 协作月报与周报

系统根据经过验证的工作项、资源版本、代码提交/评审更新和用量聚合，生成个人、项目和组织级周报/月报草稿。

报告草稿明确标记为 AI 生成，列出数据时间窗、可追溯来源、缺失数据和计算口径；发布者必须确认后才能将其共享给其他成员。

成员可以选择订阅个人周报，项目经理可以按项目生成周报，组织管理员可以生成组织月报。订阅和共享对象均遵循成员权限与通知偏好。

**月报不得自动宣布人员评价、晋升建议、淘汰建议或排名结论。** 它只描述可验证的协作事实、成本趋势、风险、阻塞项和待确认事项。

### 25.4 同层级效率排行

排行只在同一比较组内计算。比较组由组织配置的 `roleFamily`、职级区间、项目类型、工作项类别和统计时间窗共同确定。

**项目经理、开发者、设计者、运维和 Bot 服务账号永不进入同一排行榜。**

比较组必须同时满足最少有效成员数和每位成员最少完成工作项数；**不满足时只显示个人趋势和匿名分布，不显示名次**。

每个完成工作项先由负责人确认复杂度和工作类型，开始后修改必须写明理由并进入审计；无法确认复杂度的工作项不进入排行。

同层级效率分只使用以下可解释、可复核的维度：

| 维度 | 默认权重 | 计算原则 |
|---|---:|---|
| 交付质量 | 45% | 已通过评审、验收后返工率和阻塞恢复情况；质量不足不能被速度抵消 |
| 周期效率 | 35% | 实际活跃执行时长相对同类工作项的期望时长；不使用日历在线时长 |
| 资源效率 | 20% | 在相同工作项复杂度和角色组内，归一化后的模型成本与 token；更少资源不代表牺牲质量 |

默认权重属于 `OrganizationAnalyticsPolicy`，**必须**由组织管理员配置并记录生效版本；**项目经理不能为某个成员临时改权重**。

排行使用分位数或标准化分数，并展示分数构成、样本量、时间窗和数据缺口；不得只展示一个不解释来源的总分。

成员可以查看自己的全部明细并提出数据更正；项目经理只能在其授权项目内查看具名排行；组织可选择完全关闭具名排行或仅公开前后分组而不公开名次。

> **排行结果只能用于协作改进和资源规划。系统不支持把它自动写入人事档案、触发惩罚、调整薪酬或变更角色。**

---

# 五、服务端运行面

本篇定义 relay 的内部结构、部署演进与数据持久化。核心原则是单事务加 outbox、按组织分片、不静默丢数据。

## 26. 服务端闭环与写入协议

relay 是**多租户的组织协作服务，不是单个内存转发脚本**。每个业务记录都带不可变 `OrganizationId`，**请求的 token、设备签名、路径目标和数据库行的组织 ID 必须一致**。

服务端由边缘接入层、无状态 API 层、持久化业务层、异步任务层和对象存储层组成。P0 可以在单节点上部署这些逻辑组件；团队版和企业版按组织 ID 分片，但不改变协议和权限语义。

```text
边缘接入层           TLS 终止、证书固定支持、DDoS/WAF、请求上限、IP 风险初筛
认证与设备服务       账号、密码验证、设备公钥/指纹、令牌、签名证明、邀请码和恢复
会话与状态服务       浏览器会话、设备会话、长连接租约、心跳、在线聚合和风险管制
组织与策略服务       组织 ID、套餐、成员、角色、ACL、项目、确认挑战、配额和预算判定
消息与投递服务       私聊队列、群日志、成员游标、租约、ACK、去重和 E2E 信封路由
内容服务             附件预约、对象存储、资源版本、下载授权、加密、缓存和清理
仓库集成服务         仓库绑定、凭证保管、webhook 验签、提交验证和更新事件
协作服务             工作项、共享对象、协作会话、执行租约和会话隔离
通知服务             收件箱、偏好、outbox、在线推送、长连接和补拉
Bot 调度服务         Bot 策略校验、调用队列、隔离会话、Bot host 路由和结果回写
插件目录服务         提交、审核、签名验证、发布、启用、升级、撤销和能力控制
分析服务             用量归集、成本预算、报告草稿、比较组和排行计算
审计与运维服务       审计事件、黑名单、限流、分片、备份、恢复、指标和密钥轮换
```

一个会改变业务状态的 HTTP 命令**必须**遵循“认证、授权、版本检查、同一数据库事务写入领域对象和 outbox、提交后异步投递”的顺序。

**请求在事务提交前失败时返回错误且不产生任何可见领域状态**；事务提交后网络连接断开时，调用方用**同一幂等键**查询最终结果。

outbox 任务可以重复执行，因此消费方以事件 ID 去重；**通知、SSE 推送、Bot 调度和媒体清理不能反向决定原事务是否成功**。

所有异步任务具有 `queued`、`running`、`retrying`、`succeeded`、`failed`、`cancelled` 或 `dead_letter` 状态，含尝试次数、下次重试时间、最后错误和关联领域对象。

可重试错误包括网络短暂失败、可恢复数据库锁冲突和 Bot host 暂时不可用；**权限拒绝、schema 无效、配额不足、资源不存在和版本冲突是终态错误，不能自动重试**。

Bot host 处理完成后只能提交一个带调用 ID 的结果命令。relay 检查调用仍有效、Bot 仍是项目成员、输出仍符合大小和策略限制后，才把结果写成群消息或共享产物。

relay 重启时从数据库恢复未 ACK 队列、未完成上传、未完成 outbox、未完成 Bot 调用和租约；过期租约会被安全释放或重投，而不会把 `running` 永久卡住。

服务端需要分别记录用户可见错误码和运维诊断。**用户错误码不得泄露其他组织、成员、文件或群的存在性。**

## 27. L0 至 L3 渐进部署

系统按共享协议与可替换服务实现分层演进；后一层替换已定义的服务提供者，不复制或分叉领域规则。任何层都必须执行同一套认证、授权、幂等、审计、保留与恢复语义。

| 层级 | 交付包 | 运行与依赖 | 覆盖范围 |
|---|---|---|---|
| L0 契约层 | `@dsh-chat/contract` | 纯 TypeScript；零运行时依赖 | 品牌化 ID、命令、事件、错误码、状态机、schema 输入/输出与服务接口；无副作用，浏览器和 Node 均可引用 |
| L1 社区 bundle | `@dsh-chat/kernel` | 单 Node 进程 + SQLite | 装载身份、组织、消息、通知与审计等基础插件，形成单组织文本协作；附件、邮件、外部 webhook、Bot 执行和高可用接口明确返回 `NOT_IMPLEMENTED` |
| L2 团队 bundle | `@dsh-chat/team` | 无状态 API 实例 + PostgreSQL + Redis + 对象存储 + SMTP | 以配置层替换存储、nonce、通知和内容提供者，并加入附件、邮件、基础限流、备份和恢复插件 |
| L3 企业 bundle | `@dsh-chat/enterprise` | 分片 API 集群 + PostgreSQL 分区/副本 + 对象存储 + KMS/HSM + 专用安全服务 | 在不改动领域消费者的前提下替换或增挂 KMS、隔离执行、受控出站、审计归档和目录同步插件 |

`@dsh-chat/contract` 只定义类型和接口，不携带数据库驱动、HTTP 框架或业务副作用，压缩产物目标小于 100 KB。`kernel`、`team` 和 `enterprise` 都是 bundle，不承载业务单例；它们只排列插件、提供默认配置并选择提供者。**L1 的 SQLite schema 从第一版起包含 `OrganizationId`、事件 ID、操作 ID、策略修订、账户同步序列、加密元数据和恢复水位字段**，避免把后续多租户、E2E 或恢复需求变成破坏性表结构重写。

L2 通过服务注册替换 L1 的 SQLite 存储、内存 nonce、同步通知和本地附件实现，例如替换为 PostgreSQL 存储、带 TTL 的 Redis nonce、事务 outbox 和对象存储；领域命令、错误码与权限判定不因部署层改变。L3 的 KMS、沙箱、审计和出口代理由 API/worker 通过受认证的内部服务接口显式调用，不采用可被应用代码绕过的“劫持所有 fetch”或通用 gRPC 拦截器。

## 28. 可靠投递流程

可靠性语义是：**从 relay 接收消息开始，到接收方把消息写入持久化存储为止，至少投递一次**。

1. 浏览器把消息提交给 host。
2. host 校验输入，在一个本地事务中写入 `status = pending` 的发送记录，然后向 relay 发送。
3. relay 认证设备并授权目标。私聊在一个事务中提交已接收消息和目标收件人队列项；群聊在一个事务中提交消息、当前成员版本快照和单条 `GroupLog` 记录，不为每位成员同步插入队列项。
4. 发送方 host 把记录改为 `accepted`；它不能声称消息已送达或已读。
5. 私聊接收方 host 拉取一个带租约的批次，在一个本地事务中写入此前未见的消息和已接收投递记录，然后 ACK 这些 `DeliverySeq`；群成员按自己的 `GroupMessageSeq` 游标补拉有权读取的 `GroupLog` 记录并持久化游标。
6. 如果接收方在 ACK 前崩溃，租约到期后 relay 重投。同一投递的本地接收记录让再次写入幂等，host 随后 ACK。

私聊目标队列容量不足时，relay **必须在接收新消息前**以 `RECIPIENT_QUEUE_FULL` 拒绝发送。群消息由群日志配额约束；配额耗尽时返回 `GROUP_LOG_CAPACITY_EXCEEDED`，并由管理员通过保留策略或容量扩展处理。

**relay 不得淘汰已经接收但尚未 ACK 的私聊消息，也不得跳过尚在保留期内的群日志序列，为后续流量腾出空间。**

relay 按已公布的保留期保存未 ACK 消息；**管理员删除队列或保留期到期时，系统必须让发送方看见终态失败**。

每个设备同时只允许一个有效的租约拉取批次，而不是每个账号一个批次，从而为未来的多设备支持保留清晰的所有权。

### 28.1 流代次与分叉检测

relay 为每个私聊队列分区和群日志分区维护**不可回退**的 `StreamEpoch` 与 `HighWatermark`，host 在同步请求、ACK 和账户阅读水位中同时携带两者。数据库恢复、主从切换或分片故障转移**若不能证明恢复副本至少包含已公布的高水位，就不能直接恢复为可写**；服务进入 `read_only_recovery`，从持续日志补齐，或提升 epoch 并要求 host 从已签名检查点重新对账。

host 发现服务端 epoch 变化、服务端高水位低于本地已确认水位、或同一序列的消息摘要不一致时，进入 `sync_diverged`，停止 ACK、自动重发和新的组织写入，只上报本地已确认操作 ID 与检查点供 relay 对账。对账结果只能是补回缺失事件、明确标记尚未提交的本地发送失败，或由用户确认恢复副本；**不得静默把 `seq 100` 当作从未发生**。

## 29. 本地与 relay 持久化

P0 的 host 和 relay **都必须使用事务型数据库**；反复重写的 `state.json` 不足以保证消息队列、去重和附件元数据的一致性。

host 数据库存储账号/设备引用、加密恢复包引用、登录方式引用、固定 relay 指纹、组织切换状态、在线可见性偏好、风险受控状态、联系人、群快照、群日志游标、历史授权、仓库链接缓存、消息、接收投递记录、发送状态、附件/资源授权元数据、个人用量摘要和同步游标。

凭证、设备私钥、E2E 密钥包和 relay 指纹**优先存放在操作系统提供的受保护存储**；不具备该能力时，以用户提供的秘密加密后落盘，**且绝不通过浏览器 API 返回**。设备进入 `restricted` 时立即清除该组织的会话 token、解密材料和敏感缓存。

附件字节存放在受访问控制的数据库外目录；附件元数据和“写入完成”标记与让消息可见的状态处于同一事务中。

relay 数据库存储账号、密码验证值、公钥/指纹、第二验证因素与备用码哈希、恢复公钥与守护人策略、设备状态、token 撤销记录、nonce、风险事件、IP/粗粒度区域历史、在线可见范围、组织成员关系、策略修订、确认挑战、黑名单、仓库绑定与 webhook 验证配置、代码更新记录、工作项与依赖关系、评审记录、评论与修订、候选产物、联系人请求、有向拉黑记录、群事件、群日志、线程序列、表情回应、成员游标、已读水位、`StreamEpoch`、`HighWatermark`、已签名同步检查点、历史授权、已接收私聊消息、队列项、租约、ACK 记录、附件预约、`BlobObject`、`MessageContentGrant`、资源版本、保留策略、缓存失效事件、搜索索引条目、`AuditEvent` 与哈希链锚点、导出与注销请求状态和异步任务状态。

### 29.1 迁移策略

数据库 schema 版本单调递增，迁移必须使用真实历史数据库测试。迁移分为扩展、双读/双写、回填校验、切换读取、收缩五步：先增加可空字段/新表/新索引和版本化 API，再由新旧代码同时写入，后台分批回填并校验计数、摘要和外键，灰度切换读取端，最后仅在所有部署版本与备份恢复窗口越过兼容期后删除旧字段。

**禁止把生产升级设计为长时间锁表的整表 `ALTER TABLE`。** 大表新增索引使用在线/并发能力或影子表切换；迁移任务有批次上限、进度游标、可暂停、可重试和回滚读路径。**P0 即保存 `encryption_meta`、事件格式版本、设备/账户同步状态和可选扩展表**，而不是在 P4 到来时重写消息主表；**预留字段不是未定义 JSON 垃圾桶，字段语义、所有者和解析版本必须在 L0 协议中明确**。

备份必须加密并演练恢复；保留和删除规则必须区分本地历史、relay 投递数据、媒体、审计元数据、风险记录、密钥包、缓存和运行日志。**KMS/HSM 密钥、数据密钥和设备私钥不写入同一备份明文。**

## 30. 性能、分片与限流

P0 单节点按数据库事务、对象存储流式处理和有限队列运行。扩展部署按 `hash(OrganizationId)` 路由到组织分片，**同一组织的策略、成员、群和资源元数据保持在同一主分片，避免跨组织事务**。

私聊消息队列按 `OrganizationId + recipientAccountId` 分区；群消息按 `OrganizationId + GroupId` 追加日志并由成员游标读取，避免大群单条消息的同步扇出写热点。资源对象按组织前缀分桶，分析任务按组织和时间窗分片。跨组织仪表盘只读取脱敏的预聚合结果，不扫描消息或资源主库。

单个请求、连接、账号、设备、IP、组织、群、Bot、插件和上传预约均有独立 token bucket、并发上限、字节上限和 CPU/数据库时间预算。限流结果包含 `retryAfter`，但不泄露其他成员或组织的使用量。

### 30.1 限流与配额基线

以下默认值属于版本化的 `PlanLimits` 与组织策略，集中列出以避免各插件自行硬编码；表中数字是起始基线，实现必须从配置读取：

| 维度 | 默认基线 | 作用范围 | 超限行为 |
|---|---|---|---|
| 消息发送 | 30 条/分钟，突发 10 | 账号 × 组织 | `RATE_LIMITED` 含 `retryAfter` |
| 消息正文长度 | 8 000 字素簇 | 单条消息 | 提交前拒绝，不截断 |
| 评论正文长度 | 8 000 字素簇 | 单条评论 | 同上 |
| 表情回应 | 60 次/分钟 | 账号 × 组织 | 限流，不影响消息发送 |
| 附件单文件 | 自建 100 MB；团队版 2 GB；企业版按策略 | 单次上传 | 预约阶段拒绝 |
| 上传并发 | 3 个预约 | 设备 | 排队，不新建预约 |
| 搜索查询 | 10 次/分钟，单次最多 50 条结果，分页深度 500 | 账号 × 组织 | `RATE_LIMITED` |
| 认证与第二因素失败 | 10 次/小时后指数退避 | 账号与设备分别计 | 提升风险评分，可触发 `SecurityRiskEvent` |
| 联系人请求 | 20 次/天 | 账号 | 限流且不提示对方是否拉黑 |
| 导出请求 | 2 次/天 | 账号 × 组织 | 排队或拒绝 |
| Bot 调用 | 按 `PlanLimits` 的并发与预算 | 群 × Bot | `BOT_BUDGET_EXCEEDED` |
| 沙箱执行并发 | 1 个可写租约/资源版本 | 资源版本 | 只读、等待或另建分支 |
| 长连接 | 每设备 1 条组织事件流 | 设备 | 拒绝新建，保留既有 |
| 请求体 | 1 MB（不含流式上传） | 单请求 | 边缘层拒绝 |

限流按已认证账号、设备、目标，以及 relay 能可靠识别的 IP 分别计量；同一操作命中多个维度时取最严格者。**限流是保护措施而非授权判定，因此不能替代权限检查，也不能因限流放宽任何 ACL。**

边缘层过滤畸形 HTTP、协议降级、请求走私、重复 header、超大压缩体、慢速连接和已知恶意网络；应用层限制 JSON 深度、数组长度、分页大小、正则复杂度和数据库查询成本。

所有文本、富文本、文件名、URL、仓库 webhook、插件 manifest、AI 输出和外部工具结果**都作为不可信输入**。服务器执行 schema 校验、参数化查询、URL allowlist、内容类型/魔数验证、签名验证和输出编码；客户端使用安全 DOM 渲染。

> **Bot 和 AI 上下文中的外部内容必须带来源标签，不能把其中的指令当作系统权限。** 公共插件在隔离进程/沙箱中运行，网络、文件系统、秘密、模型和组织 API 访问都由声明能力和运行时策略双重限制。

---

# 六、安全与合规

本篇是安全评审的主入口。设计取向是把安全约束编码进数据模型与状态机，使不安全的操作在类型与事务层面无法表达。

## 31. 标识、租户与会话隔离

`OrganizationId`、`WorkspaceId`、`ProjectId`、`GroupId`、`ResourceId`、`CollaborationSessionId`、`BotId`、`AccountId` 和 `DeviceId` 都是**服务端生成的品牌化不透明 ID，不能由显示名、仓库 URL 或客户端随机字符串替代**。

> **所有数据库主键、唯一索引、队列分区键、对象存储路径、缓存键、审计事件和异步任务都以前缀 `OrganizationId` 隔离。** 跨组织查询在 SQL 层、ORM/查询构造层和授权层**都**必须显式带组织条件。

会话分为四类：浏览器到 host 的本地会话、host 到 relay 的设备会话、组织资源访问会话、以及协作/Bot DSH 会话。

浏览器会话只在 loopback 或受认证的 DSH Web 连接中有效，使用 `HttpOnly`、`Secure`、`SameSite=Strict` cookie 与 CSRF token；**它不能直接携带 relay access token**。

设备会话绑定 `AccountId + DeviceId + keyFingerprint + tokenId`，有短访问期、可轮换刷新期和 token 撤销列表。刷新、设备添加、密码重置、导出、组织切换和高风险动作都要求重新签名证明。

组织资源访问会话额外绑定 `OrganizationId`、成员状态、角色/ACL 修订号和风险状态。资源缓存命中前比较该修订号；成员被移除、权限收回、组织挂起或设备受控时，旧会话立即无效。

协作会话和 Bot 会话使用独立 DSH `SessionId`、独立存储目录、独立工具许可、独立预算和独立密钥引用。**它们不能读取个人 DSH 会话，也不能通过共享内存、缓存键或日志访问另一个组织的上下文。**

## 32. 授权链、强制确认与黑名单

服务端授权按**固定顺序**执行：验证 TLS/relay 指纹，验证设备签名和 token，检查 token/设备/IP/账号黑名单，检查风险状态和套餐限制，解析 `OrganizationId`，检查成员状态，计算角色与资源 ACL，检查资源版本/有效期，最后执行领域状态机。

任何一层拒绝都返回最小错误信息；**对于调用者无权知道是否存在的对象，统一返回 `NOT_FOUND_OR_FORBIDDEN`**。

强制确认使用一次性 `ConfirmationChallenge`，绑定操作者、设备、组织、动作、资源摘要、策略版本、过期时间和幂等键。高风险动作包括新增/恢复设备、改变密码或认证方式、导出组织资源、共享私人会话、开启写仓库自动化、发布高权限插件、启用公共 Bot、删除组织、修改预算或关闭安全策略。

> **挑战只有经设备签名和用户界面明确确认后才能消费；页面刷新、重复请求或网络重试不能绕过确认。** 过期挑战返回 `CONFIRMATION_EXPIRED`，必须重新展示摘要。

黑名单分为 token、设备指纹、账号、IP/CIDR、自治系统网络、仓库来源、插件摘要和恶意附件摘要。每条记录有来源、范围、原因、创建者、过期时间、申诉状态和审计事件。

命中账号、设备或 token 黑名单时立即撤销会话；命中网络或来源黑名单时按策略拒绝请求、提高风险等级或要求二次验证。普通用户不能通过错误信息枚举黑名单内容。

## 33. 连接、外部网络与 SSRF 防护

### 33.1 短连接与长连接

创建、读取、上传预约、ACL 判定和幂等查询使用短 HTTPS 请求，带严格的请求大小、超时、并发和响应大小上限。

消息拉取、通知和在线状态使用 host 到 relay 的长轮询或 HTTP/2 流；浏览器只连接 host 的同源事件流。浏览器断开不会中断 host 的消息投递循环。

每条长连接都有 `ConnectionId`、设备会话、组织范围、最后活动时间、最大存活期、心跳截止时间和取消信号。状态为 `opening`、`active`、`draining`、`closed`、`expired` 或 `rejected`。

连接超过最大存活期、权限修订变化、设备撤销、风险管制、组织挂起或服务器平滑重启时进入 `draining`，停止接受新订阅，发送可恢复原因并关闭；客户端用同一设备会话重新认证和补拉，不依赖连接内存保存业务事实。

长连接只传递事件提示和有限批次，不能代替持久收件箱、消息队列或资源下载。连接背压超过阈值时丢弃可重建的提示帧、保留游标并要求客户端补拉；不得丢弃已接收消息或授权变更。

### 33.2 受控出站

仓库验证、webhook 注册、插件源拉取、恶意文件扫描回调和 Bot 的外部工具调用**全部经由 `EgressService`，业务代码不得直接使用通用 HTTP 客户端**。每次出站请求都记录发起服务、组织、用途、目标来源策略、认证材料引用、解析结果和审计 ID；**请求体、响应正文、token 与 webhook secret 不写入普通日志**。

`EgressService` 只允许组织策略列出的协议、端口、主机名和已批准 Git 提供方。它在连接前解析 DNS，**拒绝 loopback、私有地址、链路本地地址、组播、保留地址、IPv6 本地地址、Unix socket 和非 HTTP(S) scheme**；**实际建连时再校验目标 IP，阻止 DNS rebinding**。所有重定向逐跳重新执行上述校验，**禁止由 URL 中的用户名、代理环境变量或自动凭证转发改变目标**。

L3 的出口代理是 `EgressService` 的**唯一网络出口**，而不是仅靠 `HTTP_PROXY` 环境变量。**运行身份没有直连外网权限**；代理按服务身份和 allowlist 建立受限连接、设置连接/响应大小/重定向上限并验证 TLS。**自建 Git 服务只能由管理员登记为受控私网端点**，并在独立网络策略、专用连接器和审计下访问，不能通过普通仓库 URL 绕过私网封锁。

## 34. 风险管制与账号接管防御

relay 为每个设备保存近期 IP 前缀、自治系统网络、粗粒度国家/区域、首次/最后出现时间和风险评分；不把精确地理位置用于产品展示或绩效分析。

新国家/区域、新自治系统网络、短时间不可能移动、代理/匿名网络、指纹变化、重复签名失败或异常流量会创建 `SecurityRiskEvent`，状态为 `observed`、`pending_confirmation`、`restricted`、`resolved`、`suspended` 或 `false_positive`。

达到组织策略阈值时，设备**立即进入 `restricted`**：relay 撤销该设备的组织访问 token，关闭其长连接，阻止项目、群、公共插件、共享资源、仓库、Bot 和分析数据的读取或写入。**host 收到该状态后清除该组织的解密材料和敏感缓存，只展示安全处理页**。

relay 向设备所有者和组织管理员写入高优先级通知，并向账号已验证邮箱发送异地登录安全邮件。邮件包含设备名称、指纹后缀、粗粒度地区/网络、时间、风险原因和一次性安全处理链接，不包含 access token、设备私钥、完整 IP 或组织资源信息。

安全处理链接状态为 `issued`、`opened`、`authenticated`、`consumed` 或 `expired`，绑定账号、风险事件、邮件验证状态、过期时间和一次性随机值哈希。**邮件扫描器或打开链接本身只会使其变为 `opened`，不会自动批准或撤销设备。**

用户打开链接后进入 relay 的安全中心，必须用密码、已授权设备签名、企业单点登录或配置的第二验证因素完成认证。认证成功后展示明确的“停止此异地设备”快捷按钮和风险摘要。

点击该按钮会在一个事务中将目标设备改为 `revoked`、撤销其 token、关闭其长连接、取消其未提交写租约、发布缓存失效和审计事件；随后向用户和组织管理员发送“设备已停止”通知。该动作不撤销当前已认证的处理设备。

用户也可以标记可信网络、批准一次性恢复或请求管理员处理；管理员可暂停设备，但不能代替用户绕过必须的身份确认。

在风险处理完成前，该设备不能看到任何组织项目和公共内容。超过处理期限或持续异常时，设备进入 `suspended`，只能进行恢复、导出个人安全记录和联系管理员的最小操作。

地理位置误差、旅行和企业 VPN 是预期情况，因此策略使用可配置风险评分和申诉路径；**“新地区”本身不是身份认证，也不能绕过设备签名和组织权限**。

## 35. 加密与密钥

P0 到 P3 **强制 TLS 1.3 或更高版本**、HSTS、受限密码套件、证书透明度检查和组织 relay 指纹固定。**证书轮换必须由旧证书密钥或组织离线根签名，并经过管理员确认；意外变化按中间人风险处理。**

服务器侧加密使用信封加密：对象、附件、备份和敏感数据库列由数据密钥加密，**数据密钥由 KMS/HSM 中的组织密钥加密**。应用进程只按最小权限取得短期解密许可，密钥访问记录审计。

P4 的私聊端到端加密使用经独立安全审计的双棘轮实现和设备预密钥包；relay 只保存设备公钥包、密文信封、必要投递元数据和不可逆的去重标识。

群端到端加密使用经审计的成员密钥管理协议。成员加入、移除、设备撤销和密钥恢复都会生成**新的 epoch**；**新成员不能解密旧 epoch，已移除成员不能解密新 epoch**。客户端在密钥指纹变化时**必须**显示安全确认，未确认时停止解密和发送。

> **端到端加密不等于服务器侧加密。** 启用后，服务器仍能执行成员授权、配额、队列、流量防护和密文保留，但无法生成基于消息正文的服务端搜索、明文审核或 AI 总结；**这些功能必须由用户在设备侧明确选择**。

### 35.1 加密模式功能矩阵

`OrganizationEncryptionPolicy` 明确定义功能矩阵，不能只用一个开关隐藏实现差异：

| 模式 | 消息正文 | 群公共 Bot | 服务端搜索/正文分析 | 可用指标 |
|---|---|---|---|---|
| `server_managed` | relay 可按权限读取 | 可用 | 可用 | 消息元数据、项目流转和策略允许的正文派生指标 |
| `e2ee_without_bot` | 仅成员设备解密 | 强制禁用 | 强制禁用 | 客户端签名的最小用量/工作项/提交元数据 |
| `e2ee_with_bot_member` | 成员设备和显式 Bot 设备解密 | 仅作为可见群成员可用 | relay 强制禁用 | 同上；Bot 只处理被策略授权给其设备的内容 |

切换到端到端模式前，relay 计算受影响群、Bot、插件、搜索索引和报表能力，向管理员展示降级清单并要求**强制确认**。**切换后，不能工作的入口从 UI、API 目录和自动任务中移除，而不是允许安装后持续报错。**

`e2ee_with_bot_member` 中 Bot 拥有独立设备指纹和可见身份，成员在加入或切换时明确确认该 Bot 能解密哪些群 epoch。移除 Bot 会触发群 epoch 轮换；Bot 不能读取轮换后的消息，除非管理员再次显式添加。

服务器私钥、组织 KMS 密钥和设备私钥**彼此独立**。**任何组件不得复用一个“共享秘密”作为 TLS、设备认证、数据加密和消息加密密钥。**

## 36. 缓存、保留与恢复

缓存只保存可重建数据，并以 `OrganizationId + policyRevision + resourceVersion` 命名空间隔离。授权结果、群花名册、插件目录、资源元数据和分析聚合可短暂缓存；消息正文、设备私钥、明文附件、密码验证材料和解密后的 E2E 内容不能进入共享缓存。

权限、成员、风险、黑名单、密钥 epoch、资源版本和组织状态变化会发布失效事件。失效事件优先于普通通知处理；缓存无法确认最新修订时必须拒绝访问或回源验证，不能继续返回过期授权数据。

### 36.1 保留策略基线

所有组织在创建时都获得明确的 `RetentionPolicy`，不能以 TBD 状态进入 P0。自建组织可调整，但默认值和最小安全值如下：

| 数据 | 自建组织默认 | 团队版默认 | 企业版 | 说明 |
|---|---:|---:|---|---|
| 已送达消息与群日志 | 90 天 | 180 天 | 合同/策略配置 | 到期前发送容量与保留提醒 |
| 未 ACK 私聊队列 | 14 天 | 30 天 | 30 至 90 天 | 到期后写发送方可见终态失败 |
| 消息附件授权 | 30 天 | 90 天 | 策略配置 | 仍被资源版本引用的 blob 不删除 |
| 资源软删除恢复窗口 | 30 天 | 60 天 | 策略配置 | 之后进入物理清理 |
| Bot/协作执行日志 | 30 天 | 90 天 | 策略配置 | 不含被禁止保留的秘密 |
| 审计与风险事件 | 365 天 | 365 天 | 合同/法规配置 | 法律保留可延长 |

`RetentionPolicy` 同时定义每类对象的字节配额、预警阈值和清理批次大小。容量达到 80% 时通知组织管理员，达到 90% 时限制新的大对象上传，达到 100% 时拒绝会增加存储的写入但不删除已有已接收数据。

清理任务分为过期租约回收、未 ACK 队列保留期处理、上传预约回收、临时对象删除、消息/附件授权到期、资源软删除转物理删除、缓存淘汰、审计归档、备份过期和分片压缩。每个任务记录扫描游标、候选对象、策略版本、执行结果和可重试错误。

**删除遵循“先不可见、后保留、再物理清理”的流程。** 受法律保留、审计冻结或未完成恢复的对象不会物理清理；**清理失败进入 `dead_letter` 并告警，而不是静默跳过**。

relay 和 host 均使用受限大小的 LRU 缓存、对象流式传输和分块校验，避免把大文件或大群历史整体装入内存。附件下载和上传支持断点续传，但每个分块都绑定预约、摘要、组织和权限。

备份采用跨可用区/介质的加密快照与持续日志，定期恢复演练到隔离环境。**恢复完成前服务处于 `read_only_recovery`，禁止写入，避免新写入和旧数据分叉。**

## 37. 审计事件模型

审计是 P0 即装载的核心能力，因此 `AuditEvent` 的结构属于 `@dsh-chat/contract`，不由各插件自行定义。

每条审计事件包含不可变 `AuditEventId`、`OrganizationId`、事件类型、发生时间、服务端序列号、操作者身份（`AccountId` 或服务账号）、`DeviceId`、来源 IP 前缀与粗粒度区域、目标对象引用、操作结果、错误码、策略版本、关联操作 ID/幂等键、关联领域事件 ID 和调用链 ID。

**审计事件记录事实与引用，不记录内容。** 消息正文、附件字节、提示词、模型输出、密码、token、密钥、webhook secret 和恢复材料**一律不进入审计记录**；需要表达对象身份时使用不可逆摘要或资源版本引用。

事件类型至少覆盖：身份与设备（注册、认证、恢复、撤销、风险状态变更）、组织与授权（成员、角色、ACL、策略、套餐、确认挑战）、内容（上传、扫描、下载授权、隔离、删除、保留清理）、协作（共享、协作会话、执行租约、产物发布）、仓库与出站（绑定、验签、提交验证、受控出站）、插件与 Bot（提交、审核、发布、启用、撤销、调用）、分析与计费（策略变更、明细查看、导出）、以及运维（迁移、恢复、水位对账、密钥轮换、黑名单）。

**审计写入与领域写入处于同一事务**：领域对象、outbox 与审计事件一并提交，因此不存在“操作成功但无审计”或“有审计但操作未生效”的状态。**审计写入失败即整个命令失败。**

**被拒绝的操作同样产生审计事件**，记录拒绝所处的授权层与错误码。这使得越权尝试可被检测，但返回给调用者的错误信息仍遵循最小披露原则。

审计存储的不可变性按部署层递进，**功能矩阵必须显式声明而不是笼统承诺“不可篡改”**：

| 层级 | 不可变形态 | 说明 |
|---|---|---|
| L1 | 仅追加表 + 应用层禁止更新/删除 | **具备数据库权限者仍可改写；文档不得宣称防内部人篡改** |
| L2 | 仅追加 + 每日哈希链锚点 + 独立备份 | 可检测事后批量篡改，锚点与主库分离存储 |
| L3 | 逐条哈希链 + 外部不可篡改归档 + 独立密钥签名 | 支持第三方验证与法规导出 |

L2 起每条审计事件包含前一事件摘要，形成按 `OrganizationId` 分区的哈希链；链断裂检测到时产生高优先级运维告警并进入调查状态，不自动修复。

审计查询是受限能力。审计者角色默认只读取被授权范围的审计记录与元数据，不读消息正文；查看个人分析明细、导出审计和跨项目查询本身也是被审计的动作。审计记录按 `RetentionPolicy` 保留 365 天或按合同/法规配置，法律保留可延长；审计数据不参与排行、报告与模型上下文。

## 38. 账号注销、导出与合规

账号、组织成员关系和组织数据是三个不同的所有权层次，因此退出组织、组织删除和账号注销是三条独立流程，不能互相代替。

### 38.1 数据导出

数据导出是成员的常规能力，不是注销的附属步骤。导出请求状态为 `requested`、`preparing`、`ready`、`downloaded`、`expired` 或 `failed`；导出包加密、绑定请求者、限定下载次数与有效期，生成与下载都写入审计。

导出范围按主体区分：

| 主体 | 可导出内容 | 明确不含 |
|---|---|---|
| 成员个人 | 自己发送的消息、自己上传的资源版本、自己的工作项、个人用量明细、账号与设备安全记录 | 他人私聊、未授权资源、他人用量、组织审计全量 |
| 组织管理员 | 组织结构、成员关系、策略、资源目录元数据、已授权范围内的群日志与资源 | 成员私聊、成员私人 DSH 会话、E2EE 密文正文 |
| 审计者 | 被授权范围的审计事件与元数据 | 消息正文、秘密、密钥材料 |

管理员导出是高风险动作，需要 `ConfirmationChallenge`，并向被涉及成员写入通知；组织可配置双人审批。**导出不得成为绕过资源 ACL 的旁路，导出内容按请求者当时的权限逐条裁剪。**

### 38.2 账号注销

账号注销由账号所有者发起，状态为 `requested`、`pending_transfer`、`grace_period`、`anonymized` 或 `purged`。发起注销需要重新认证与确认挑战。

注销前**必须先解决所有权阻塞**：账号是任一组织的唯一所有者时进入 `pending_transfer`，**必须先完成组织所有者转让或组织删除**；存在未结算账单、法律保留或未完成恢复流程时同样阻塞。**系统不因个人注销而使组织失去管理主体。**

注销进入默认 30 天的 `grace_period`，期间账号不可登录但可由本人撤销注销。**宽限期结束后执行匿名化而非物理删除全部记录**：

- 账号档案、邮箱、账号名、设备公钥、IP 与区域历史、第二因素与恢复材料物理删除。
- 消息与群日志正文按其所属组织的 `RetentionPolicy` 处理，作者字段替换为**不可逆的 `AccountTombstone`**，保留稳定的匿名标识以维持会话可读性与序列完整性；**不追溯删除他人已合法接收的协作事实**。
- 联系人关系与拉黑记录删除；对方界面显示为已注销账号。
- 群成员身份转为历史事件，`membershipRevision` 不回滚。
- 工作项、资源版本与已验证提交的归属转为 `AccountTombstone`，保留项目交付事实与审计完整性。
- 分析明细中的个人维度删除或聚合到匿名分组，个人排行历史移除。
- 审计与风险事件保留至各自保留期，操作者字段替换为 tombstone；受法律保留的记录延长保留。

**匿名化不可逆。** 文档必须明确：**注销不删除他人副本、已导出内容、已下载附件，也不撤销已发生的协作事实**；这与撤回和共享撤销的边界声明一致。

### 38.3 组织删除与数据导入

组织删除同样使用宽限期。发起删除需要组织所有者确认挑战与双人确认（企业版可配置），随后进入默认 30 天 `grace_period`：组织转为只读，停止新写入、Bot 调用、插件启用和外部通知，保留导出与恢复能力，并向全体成员与账单联系人发送通知。宽限期内可由所有者撤销。宽限期结束后按对象类型执行物理清理，审计与合规记录按法规保留期单独留存；清理进度可查询，失败进入 `dead_letter` 并告警。

自建组织的部署管理员可以直接删除底层数据库与对象存储，这超出应用层承诺范围；文档不得暗示应用层删除流程能约束具备基础设施权限的运维者。

数据导入与跨平台迁移**不在 P0 至 P3 范围内**。企业迁移需求列入开放决策，需先确定导入范围、作者映射、去重规则、权限重建、历史授权推导和导入内容的审计标记方式；**在此之前导入入口显示为未安装**。

## 39. 安全规范清单

以下条目是对前述各节的强制性汇总，用于评审逐条核对。

- relay 通信使用 HTTPS；**HTTP 只允许在明确的 loopback 开发模式下使用**。
- host 固定组织 relay 的 TLS 证书/公钥指纹；**未被旧密钥或组织离线根签名确认的轮换一律视为中间人风险，不允许降级连接**。
- 浏览器写路由要求同源校验和每个会话的 CSRF token；**开发模式外，缺失或 `null` 的 Origin 一律拒绝**。
- **token、refresh credential、私钥、原始媒体路径、relay 注册邀请码不得出现在浏览器状态、URL、日志、遥测或错误信息中**。
- 密码只在 TLS 认证流程中使用，relay 只保存 Argon2id 验证值；密码重置、设备撤销和组织切换均需重新认证或已授权设备确认。
- 在线状态只暴露 host 存活提示，遵守成员选择的可见范围；它不记录键盘活动、页面内容或精确在线时长。
- 新地区、异常网络、指纹变化和异常流量按组织风险策略将设备置于受控状态；在重新验证或管理员处理完成前，该设备不能读取或写入任何组织项目和公共内容。
- 异地登录安全邮件使用短期单次链接；链接打开后必须重新认证，才可执行“停止此设备”，避免邮件扫描器、转发或被盗邮箱直接触发撤销。
- 所有外部请求和 relay 响应在使用前都要执行 schema 校验和请求体上限校验。
- 每次写操作都使用幂等键，并产生包含操作者、操作、目标、结果和关联 ID 的审计记录；审计记录不得包含消息正文或秘密。审计与领域对象在同一事务提交，审计写入失败即整个命令失败；被拒绝的操作同样产生审计事件。
- 高风险路径要求第二验证因素：新设备注册、账号恢复、认证方式变更、安全中心处置、组织所有权转让、组织删除和管理员导出。备用码只展示一次且只保存哈希；第二因素与 `RecoveryKit` 是独立路径，不能互相替代。
- 每次读取、下载、继续协作会话和 Bot 调用都**同时**校验组织成员状态、项目权限、资源 ACL、对象状态和有效期；**不能只根据前端是否显示入口授权**。
- **搜索索引不构成读取授权。** 每条候选结果在返回前逐条复检资源 ACL、历史授权区间、对象状态和有效期；复检失败的条目静默丢弃且不计入总数，避免以结果计数推断不可见内容。撤回、隔离、授权撤销和保留期到期必须使索引条目转为 `suppressed` 或 `purged`。
- 端到端加密模式下 relay 强制禁用正文索引；设备侧本地索引与解密材料同等保护，设备进入 `restricted` 时一并清除，界面必须显示已索引区间而不暗示结果为全量。
- 转发逐条复检原消息读取权与附件授权，复用同一 `BlobObject` 而不复制字节，且一律禁止跨组织转发；线程权限完全继承所属群与历史授权，不新建权限域。
- 评审批准只对锁定的产物版本生效，关联产物变化后自动 `superseded`；执行成功不等于产物被接受，候选产物必须经有写权限成员确认并通过版本比较才能成为当前版本。
- 评论中的 @ 提及只能指向对该对象已有读取权限的成员，提及无权限成员返回 `FORBIDDEN`，不因提及授予任何访问权；`comment` 访问级别不含改状态、发起或批准评审、再共享。
- 数据导出按请求者当时的权限逐条裁剪，不得成为绕过资源 ACL 的旁路；导出包加密、绑定请求者、限定下载次数与有效期，生成与下载都写入审计。
- 账号注销执行匿名化而非追溯删除他人已合法接收的协作事实；唯一所有者必须先完成所有权转让或组织删除，组织不允许处于无 `active` 所有者的状态。
- 通知摘要只展示接收人原本有权读取的最小信息；被撤销权限后，收件箱保留事件类型和时间，但不再展示敏感标题、内容或下载入口。
- 上传资源在 `available` 前必须完成摘要验证和组织策略要求的安全扫描；隔离、拒绝和删除操作都产生审计事件。
- 公共插件必须固定精确版本并通过组织允许的来源、签名和依赖策略；插件读取秘密、访问外网、调用模型或写项目资源前都要有显式能力授权。
- 仓库访问 token、webhook secret 和部署密钥只存入 relay 的受保护秘密存储；浏览器和普通群成员只能获得跳转链接与已授权的提交元数据。
- 代码提交归因必须经过远端仓库验证；客户端上报的 hash、AI 使用标签和变更统计都不能单独进入排行榜或月报。
- **Bot、AI、插件、仓库和外部工具输出一律按不可信内容处理**；只有客户端显式提交的交互意图能进入命令或工具执行路径。
- 分析事件只采集计算指标必需的最小元数据；**私人聊天、原始提示词、文件正文和私人会话不进入组织排行榜或报告训练上下文**。
- 个人分析明细、数据更正和排行可见性受组织保留期与访问策略保护，管理员的查看和导出行为必须审计。
- 输入限流按已认证账号、设备、目标，以及 relay 能够可靠识别的 IP 执行。
- relay 健康检查端点要求运维认证，或只输出不敏感的聚合就绪信息。
- 部署管理台与 CLI 走同一套认证、确认挑战与审计路径，**不设置绕过授权链的后门接口**。
- **日志与指标不得包含消息正文、附件字节、提示词、模型输出、token、密钥、webhook secret、完整 IP 或恢复材料**；查询词按组织策略脱敏且不写入普通日志。
- 向 Bot 传输上下文时必须让调用者可见，并遵循该 Bot 选择的保留策略。
- **P4 端到端加密使用经过独立安全审计的密码学库和协议；禁止自行实现棘轮、群密钥协商、证书校验或随机数算法。**

---

# 七、可观测性与运维

本篇面向 SRE 与部署管理员，定义必须暴露的指标、服务等级目标与升级顺序。

## 40. 可观测性、SLO 与容量目标

relay 是权威运行面，因此可观测性属于交付范围而不是运维自选项。指标、日志与追踪都以 `OrganizationId` 打标，但跨组织仪表盘只读取聚合结果。

必须暴露的指标面至少包括：私聊队列深度与最老未 ACK 年龄、群日志写入速率与成员游标滞后、outbox 待处理量与 `dead_letter` 计数、租约持有与过期回收量、长连接活跃数与 `draining` 比例、索引滞后、异步任务各状态分布、分片健康与主从延迟、`StreamEpoch` 与 `HighWatermark` 推进情况、限流命中率、认证失败与风险事件速率、存储与配额水位、模型调用成本与预算消耗。

日志规范集中如下：结构化输出，携带请求 ID、调用链 ID、`OrganizationId`、操作 ID 与错误码；消息正文、附件字节、提示词、模型输出、token、密钥、webhook secret、完整 IP 与恢复材料一律不写入日志；查询词、文件名与仓库 URL 按组织策略脱敏。运维诊断日志与用户可见错误码分离存储。

### 40.1 服务等级目标

服务等级目标按部署层给出数量级，作为验收与容量规划基线而非合同承诺：

| 指标 | L1 单节点 | L2 团队版 | L3 企业版 |
|---|---|---|---|
| 支撑账号数 | 32（默认上限，可由部署管理员调整） | 数千 | 按分片线性扩展 |
| 活跃群数量级 | 百 | 万 | 十万 |
| 私聊投递端到端 p95（双方在线） | < 2 s | < 1 s | < 1 s |
| 群消息可见 p95 | < 3 s | < 1.5 s | < 1.5 s |
| 通知收件箱写入 p95 | < 2 s | < 1 s | < 1 s |
| 同步游标滞后 p95 | < 5 s | < 3 s | < 3 s |
| 单群成员上限 | 不超过该部署的账号上限 | 500 | 按合同与区域部署确定 |
| 可用性目标 | 无 | 99.5% | 99.9% 起，按合同 |
| RPO | 最近一次备份 | ≤ 5 min | ≤ 1 min |
| RTO | 人工恢复 | ≤ 4 h | ≤ 1 h |

RPO 与“已公布高水位”直接相关：**任何声明的 RPO 都不得小于恢复副本能证明包含的高水位间隔，否则必须进入 `read_only_recovery` 而不是按 RPO 宣称无损。**

表中的容量数字是数量级基线，实际强制值仍由版本化的 `PlanLimits` 配置决定；**两者冲突时以 `PlanLimits` 为准，本表不得被实现读作代码常量。**

P0 验收即需记录上述基线的实测值；**达不到数量级时必须在文档中修正目标，不允许以“后续优化”留空**。

## 41. 协议版本协商与升级顺序

host 与 relay 独立升级，因此协议兼容性必须显式协商而不是靠同步发版。

`@dsh-chat/contract` 声明 `ProtocolVersion`（单调递增整数）、最低支持版本和各事件的 `eventFormatVersion`。host 在建立设备会话时提交自身协议版本与已支持事件格式集合；relay 返回协商结果、服务端当前版本、最低支持版本和弃用截止时间。

协商失败返回 `PROTOCOL_VERSION_UNSUPPORTED`，**host 显示明确的升级提示并停止组织写入，不进入静默降级或部分可用状态**。

兼容窗口默认为两个次要版本或 90 天中的较长者。窗口内 relay **必须**同时接受旧版本命令并以旧事件格式向旧 host 投递；窗口结束前向管理员与受影响成员发出通知。

**升级顺序固定为 relay 先升、host 后升**：relay 必须先具备同时服务新旧协议的能力，再允许 host 升级。**新版本 relay 不得要求所有 host 已升级才能启动。** 数据库迁移遵循扩展、双读/双写、回填校验、切换读取、收缩五步，且收缩步骤只能在所有部署内 host 版本与备份恢复窗口都越过兼容期后执行。

部署管理台或 CLI 至少提供：版本与协商状态查询、迁移进度与暂停、分片与水位健康、outbox 与 `dead_letter` 处置、黑名单与限流调整、备份与恢复演练触发、密钥轮换、组织挂起与保留策略调整。**管理台操作全部走同一套认证、确认挑战与审计路径，不设置绕过授权链的后门接口。**

---

# 八、交付计划与验收

本篇是排期与验收的依据。每个阶段都给出交付范围、用户闭环与必须覆盖的失败路径。

## 42. 关键操作的成功与失败状态

| 操作 | 成功提交的最小条件 | 可重试情况 | 终态失败 |
|---|---|---|---|
| 创建组织 | 创建者账号有效；组织名通过校验；组织、所有者成员记录和审计事件在同一事务提交 | 网络超时后按幂等键查询 | 名称不合法、账号冻结、配额不足 |
| 恢复自建组织账号 | 恢复包加口令或守护人阈值通过；新设备密钥注册；旧设备冻结 | 守护人暂时离线、邮件/第二验证服务短暂失败 | 恢复证明无效、阈值不足、组织恢复包缺失、风险策略拒绝 |
| 邀请成员 | 邀请者有成员管理权；目标可被邀请；邀请记录和通知提交 | 通知或在线推送失败 | 无权限、目标已移除、邀请码过期、组织已挂起 |
| 切换组织 | 账号是目标组织有效成员；组织策略和权限快照已加载 | host 缓存同步短暂失败 | 成员无效、组织挂起、凭证过期 |
| 更新在线状态 | 已认证设备心跳通过；状态可见范围符合个人设置 | 网络短暂失败，下一次心跳重试 | 设备撤销、账号冻结、签名无效 |
| 建立长连接 | TLS/relay 指纹、设备会话、组织权限和连接额度均通过 | 服务器平滑重启、网络中断后补拉 | 指纹不匹配、设备受控、组织挂起、连接超额 |
| 处理异常地点风险 | 用户/管理员以受信任设备或第二验证因素通过挑战；风险事件完成审计 | 验证服务暂时不可用 | 挑战过期、证明无效、管理员拒绝、设备已暂停 |
| 邮件停止异地设备 | 邮箱链接有效，用户已重新认证；目标设备属于该账号且未撤销 | 邮件投递失败时在已授权设备的安全中心处理 | 邮箱未验证、链接过期、认证失败、目标设备已撤销 |
| 分派工作项 | 操作者有分派权；负责人是有效项目成员；版本匹配 | 超时后查询同一命令结果 | 版本冲突、负责人无项目权限、工作项终态 |
| 签收工作项 | 负责人有效且签收状态为 `offered`；确认理由与版本提交 | 网络超时后按幂等键查询 | 工作项已取消、负责人已变更、签收已过期 |
| 提交评审结论 | 评审人有项目读取权且非提交者；关联产物版本未变化；结论与审计提交 | 网络超时后按幂等键查询 | 产物已变化转 `superseded`、评审已过期、评审人无资格 |
| 接受候选产物 | 确认者有目标资源写权限；版本比较通过；新版本、归因与通知同事务提交 | 超时后按幂等键查询 | 版本冲突、协作会话已撤销、权限被收回 |
| 发送消息 | 发送者与目标均通过策略；私聊队列或群日志配额可用；消息与对应队列/群日志记录提交 | 网络超时、租约重投、ACK 或群游标补拉 | 无权限、被拉黑、队列满、群日志配额耗尽、组织挂起、输入超限 |
| 发布共享对象 | 共享者可读原对象；目标有效；ACL、共享记录、审计和通知提交 | 超时后按幂等键查询 | 资源已删除、目标无资格、版本冲突、访问级别超权 |
| 上传资源 | 上传预约与摘要匹配；上传者有目录写权；版本记录提交 | 分块网络失败、扫描器短暂不可用 | 配额不足、摘要不符、文件类型拒绝、恶意文件、预约过期 |
| 授予群历史 | 群管理员选择固定游标/时间范围；接收人有效；消息、附件和资源授权原子提交 | 通知失败或接收方离线 | 无权限、范围超出策略、E2E 旧 epoch 不可用、资源禁止快照 |
| 发送共享资源快照 | 资源版本可用且目录策略允许；`MessageContentGrant` 与消息原子提交 | 上传/下载短暂失败 | 资源已删除、快照被禁止、目标无资格、配额不足 |
| 绑定项目群仓库 | 操作者有项目群管理权；仓库 URL、提供方和访问模式通过策略 | 远端验证或 webhook 注册暂时失败 | URL 不允许、重复绑定、无权限、仓库认证失败 |
| 记录 AI 辅助提交 | hash 与绑定仓库匹配，且 webhook/API 验证作者和提交存在 | 远端暂时不可访问时继续验证 | hash 不存在、签名无效、作者冲突、仓库已撤销 |
| 继续协作会话 | 成员有 `continue` 权限；输入快照有效；可写资源租约可取得或已建分支 | 执行 host 短暂离线、可恢复工具失败 | 会话撤销、工作项关闭、版本冲突、权限被收回 |
| 启动沙箱执行 | `ExecutionProvider` 可用；能力、预算、仓库/资源版本和确认挑战通过 | provider 暂时不可用、可恢复镜像拉取失败 | 沙箱配额不足、网络/秘密/命令未授权、项目或仓库权限失效 |
| 调用公共 Bot | 调用者、群/项目、Bot 和策略全部有效；上下文快照在额度内 | Bot host 或模型短暂故障 | 无调用权、上下文超限、Bot 已移除、工具策略拒绝 |
| 发布公共插件 | 审核人有权限；精确版本、权限清单和审核记录已提交 | 签名或漏洞扫描服务短暂不可用 | 审核拒绝、签名不符、依赖不允许、套餐不支持 |
| 生成周报/月报 | 调用者有分析权限；数据时间窗、范围和报告模板有效 | 分析聚合尚未完成 | 数据范围无权限、样本不足、组织禁用报告 |
| 计算同层级排行 | 比较组、样本量、角色族和工作项归属均有效 | 聚合任务短暂失败、数据更正后重算 | 样本不足、跨角色比较、复杂度未确认、组织已关闭排行 |
| 端到端密钥轮换 | 所有有效设备已获取并验证新 epoch；成员变更和审计事件已提交 | 离线设备稍后补取密钥包 | 指纹未确认、设备撤销、成员权限失效、密钥包校验失败 |
| 登记第二验证因素 | 确认挑战与已授权设备确认通过；当场完成一次验证；备用码已签发并只保存哈希 | 认证器时钟偏移在容忍窗口内重试 | 挑战过期、验证失败、组织禁用该因素类型、删除后不满足强制策略 |
| 转让组织所有权 | 发起方与接收方均通过确认挑战和第二因素；接收方为 `active` 组织管理员；角色变更、审计与通知同事务提交 | 接收方暂未确认时保持待处理 | 接收方无资格、组织挂起、发起方非所有者、公示期被中止 |
| 执行搜索 | 调用者成员状态有效；查询在复杂度与速率上限内；候选结果逐条通过 ACL 复检 | 索引滞后时提示结果可能不完整并可重试 | 无成员资格、组织挂起、跨组织查询、E2E 模式下请求服务端正文检索 |
| 请求数据导出 | 请求者权限已按对象逐条裁剪；导出包加密并绑定请求者；生成与审计提交 | 打包或对象存储短暂失败 | 无权限、超出导出配额、组织挂起、法律保留冲突 |
| 注销账号 | 重新认证与确认挑战通过；无唯一所有者、账单、法律保留等阻塞；进入宽限期 | 宽限期内可由本人撤销 | 仍是唯一组织所有者、存在未结算账单或法律保留 |
| 删除组织 | 所有者确认挑战与双人确认通过；组织转只读并进入宽限期；全员通知已提交 | 宽限期内可撤销 | 非所有者、存在法律保留、订阅未结清、确认过期 |

**任何操作在返回成功前都已经写入持久化状态；返回超时不代表失败，客户端必须查询同一幂等键的结果。**

**任何操作在返回失败前都必须说明能否安全重试。浏览器不能把“未收到响应”直接渲染为失败后重新生成 ID 再次提交。**

## 43. 最小可运行骨架

只有两台 DSH Web 实例和一个持久化 relay 完成以下流程时，P0 才算完成：

1. 管理员创建两个一次性注册邀请码。
2. 两位用户各注册一个设备、设置密码或设备登录方式、登记 `totp` 与一次性备用码、导出恢复包或配置恢复守护人，并经其他渠道交换公开联系人地址。
3. 用户 A 创建组织、工作区和项目，并把用户 B 以开发者角色邀请进项目。
4. 两人启动 DSH 后互相看到 `online`；用户 B 改为隐藏在线状态后，用户 A 只看到 `unknown`，但消息投递仍正常。
5. 用户 A 创建一个工作项并分派给用户 B；用户 B 在收件箱看到持久化通知。
6. 用户 A 发送联系人请求，用户 B 接受。
7. 用户 A 在用户 B 离线时发送一条中文文本消息。
8. relay 重启，队列、通知、工作项和组织切换状态不丢失。
9. 用户 B 启动 DSH，持久化消息并 ACK，重启 DSH 后仍只看到一条消息和一条已读通知。
10. 用户 A 模拟丢失唯一设备后，使用恢复包或守护人阈值注册新设备；旧设备无法继续访问，仍在保留期内的非 E2E 数据可读取。
11. 收件人队列满时，新发送被明确拒绝，已接收的早期消息不被删除。
12. 用户 A 在本地搜索到该条私聊；服务端搜索、线程与转发入口显示为未安装。
13. 用旧协议版本的 host 连接 relay 时返回 `PROTOCOL_VERSION_UNSUPPORTED` 并停止组织写入，不进入部分可用状态。
14. 上述每一步在审计表中都有对应事件，且被拒绝的越权尝试同样留下记录；审计表中不含任何消息正文。

### 43.1 初始工程结构

```text
dsh-chat/
  DESIGN.md
  packages/
    chat/
      contract/          @dsh-chat/contract：共享品牌 ID、命令、事件、错误码、schema 与 SPI
      kernel/            @dsh-chat/kernel：L1 插件 bundle 与社区默认配置
      team/              @dsh-chat/team：L2 provider 覆盖与团队 bundle
      enterprise/        @dsh-chat/enterprise：L3 provider 覆盖与企业 bundle
      host/              dsh-chat host 插件：本地数据库、relay 客户端、同源路由、事件发布
      client/            dsh-chat client 插件：DSH slot 注册、页面、store、CSS Modules
      identity/          身份、设备、恢复、会话与风险管制插件
      organization/      组织、成员、角色、ACL、项目与策略插件
      workitem/          工作项状态机、依赖、签收、评审与评论插件
      presence/          心跳、在线可见范围与订阅插件
      messaging/         私聊、群日志、编辑、撤回、ACK、游标与投递插件
      content/           附件、资源库、扫描、授权、保留与对象存储插件
      notification/      收件箱、邮件、SSE 与 outbox 消费插件
      collaboration/     共享、协作会话、执行租约与执行 provider 插件
      repository/        仓库、受控出站、webhook 与提交归因插件
      bot/               隔离群 Bot、公共工具目录与能力租约插件
      search/            索引管线、查询授权复检与设备侧索引插件
      analytics/         用量、预算、报告、排行与计费插件
      compliance/        数据导出、账号注销、组织删除与保留合规插件
      audit/             审计事件、哈希链、迁移、水位恢复、缓存失效与运维插件
  examples/
    two-users/           两个 host、一个组织、一个项目和一个 relay 配置
```

`@dsh-chat/contract` 是唯一的共享协议包；`kernel`、`team` 和 `enterprise` 只选择其服务提供者，不重新定义命令或权限语义。

`client` 绝不访问 relay 凭证或数据库。

`host` 是浏览器面向组织、relay、协作、Bot、插件目录、分析和账单服务的唯一入口。

## 44. 迭代计划 P0–P4

| 阶段 | 主题 | 部署层 | 核心交付 |
|---|---|---|---|
| P0 | 身份、组织与文本私聊 | L0 + L1 SQLite | 身份、组织、工作项、私聊、审计、错误码、协议协商 |
| P1 | 团队运行基础、群聊与资源 | L2 PG/Redis/对象存储 | 群聊、历史授权、资源库、仓库绑定、服务端搜索 |
| P2 | 协作会话、公共群 Bot 与富文件 | + 执行 provider | 协作会话、沙箱执行、候选产物、群 Bot、线程与转发 |
| P3 | 团队版、组织治理、公共工具与分析 | + analytics/billing | 套餐限制、插件目录、成本大屏、排行、合规流程 |
| P4 | 企业版、安全、分片与端到端加密 | L3 KMS/分片 | 分片、E2EE、目录同步、审计归档、设备侧索引 |

### 44.1 P0：身份、组织与文本私聊

先交付 L0 和 L1。装载 identity、organization、presence、messaging、notification、audit、host 和 client 插件，使用 SQLite 提供者；L1 只服务一个由 `DSH_ORG_ID` 明确指定的组织。

用户闭环为：管理员创建邀请码，成员注册设备、登记 `totp` 与备用码并导出恢复材料，组织所有者创建工作区/项目并邀请开发者，开发者接受成员关系和联系人请求，双方离线/在线发送文本私聊，接收方持久化并 ACK；项目经理分派工作项，负责人签收、开始、阻塞、提交评审或完成。风险设备、撤销设备、权限收回、队列满、重复请求和 relay 重启均有可见终态。

P0 同时交付 `AuditEvent` 结构与仅追加存储、`@dsh-chat/contract` 中的错误码目录与协议版本协商、host 本地的私聊与联系人查找、工作项依赖与评论、限流基线表的配置化实现，以及可观测性指标面和 SLO 基线实测。

评审关口在 P0 即建立状态机与权限判定，但只覆盖工作项与产物版本；关联 `CodeChangeRecord` 的评审随 P1 仓库能力一并启用。

P0 不装载内容、仓库、执行、Bot、公共工具、分析和计费功能插件；服务端搜索、线程、转发、数据导入等入口必须显示为未安装或返回 `NOT_IMPLEMENTED`，不得伪装为可用。

验收必须覆盖最小可运行骨架，以及唯一设备丢失后的自建恢复、第二因素丢失后用备用码恢复、ACK 前 host 崩溃、relay 重启、重复投递、未授权发送被拒绝、队列已满、凭证重放、在线状态过期、组织切换缓存隔离、角色越权、成员被移除后的访问拒绝和跨源浏览器写请求等失败路径；还必须验证协议版本不兼容时返回 `PROTOCOL_VERSION_UNSUPPORTED` 而非静默降级、被拒绝操作同样产生审计事件、审计写入失败导致整个命令失败，以及评审批准后关联产物变化时自动转 `superseded`、工作项依赖成环被拒、限流在各维度按最严格者生效，和任一插件卸载后其路由、后台任务和事件监听不残留。

### 44.2 P1：团队运行基础、群聊与资源

交付 L2 团队 bundle，以 PostgreSQL、Redis、对象存储、SMTP 提供者替换 L1 默认实现；装载 content、repository、search 和团队版 notification 插件。实现多组织成员关系、邀请接受与过期、群主管理、群成员版本、系统事件、群日志与成员游标、历史授权与续期、引用快照、共享对象、版本化资源库、附件预约、图片/文件传输、配额和稳定下载授权；实现受限 Markdown、表情回应最小模型、群资料，以及群消息与资源元数据的服务端索引。

用户闭环为：项目经理创建群并绑定项目，成员接受邀请；管理员选择并授权所需历史，成员可按群日志补拉；成员上传文件或将资源版本作为实时引用/快照发送，接收人按自己的授权下载；项目群管理员登记仓库链接，仓库插件通过 `EgressService` 验证来源和 webhook，验证后的提交更新进入群和工作项。

验收必须使用三台 host，覆盖离线投递期间的成员变更、群事件缺口恢复、带范围的群历史授权与到期、仓库链接权限、webhook 签名错误、已验证 hash 去重、共享消息不泄露整段私聊、附件重试、资源版本快照授权、后来入群者不能读取未授权旧媒体，以及退群者在保留期内能重新下载已授权媒体；还必须验证搜索结果逐条复检 ACL 后不返回越权条目、撤回消息不在索引中残留，以及表情回应跨设备幂等同步。

### 44.3 P2：协作会话、公共群 Bot 与富文件

装载 collaboration、执行 provider 和 bot 插件。实现个人会话选择性公开、隔离协作会话、执行租约、只读输入快照、候选产物与接受流程、分支/版本合并、文件预览和受控视频；实现显式安装 Bot、隔离的 `(GroupId, BotId)` DSH 会话、调用策略、上下文冻结、工具能力、审计和结果发布；实现消息线程、转发、群公告与置顶、只读群。

用户闭环为：成员从私人会话选择指定消息、文件或产物创建协作会话，指定继续开发的人和范围；接手者取得只读快照或可写租约，在沙箱执行后提交候选版本，原始资源只有在版本检查和人工确认后更新。群主从已审核目录添加 Bot，成员显式 @ 或使用 UI 命令调用，Bot 在允许的群/工作项/资源范围内运行并作为 Bot 身份发布结果；Bot 产物先进入草稿或经确认后公开。

验收必须证明协作会话不包含未选择的私人上下文，沙箱无法访问 host 文件系统/环境变量/未授权网络且会在配额超限时终止，并发继续开发不会覆盖资源版本；群 Bot 无法访问成员私人 DSH 会话，未满足触发条件时不会运行，Bot/AI 输出不能触发二次命令解析，工具权限、上下文限制和速率限制都能生效；还必须验证线程不扩大群可见范围、转发逐条复检原消息读取权且跨组织被拒、转发复用同一 `BlobObject` 而不复制字节，以及执行成功但候选产物未经确认时不改变资源库当前版本、产物在尝试期间被他人推进时返回 `VERSION_CONFLICT`。

### 44.4 P3：团队版、组织治理、公共工具与分析

装载 analytics、billing 与公共工具目录插件。实现自建组织和团队版 `PlanLimits`、订阅状态、公共插件的提交/审核/发布/撤销、成本大屏、个人 token 工作台、预算告警、周报/月报、已验证代码更新和同层级可解释排行；实现第二验证因素的组织强制策略与按角色分级、数据导出流程、账号注销与组织删除的宽限期，以及工作项、代码更新与插件目录搜索。

用户闭环为：管理员设定成员、存储、Bot、预算和保留限制；发布者提交固定版本和能力声明，审核者批准后成员一键启用；成员的调用、资源和已验证提交进入最小化分析事件，个人查看自己的消耗和更正入口，经理查看本项目聚合数据，组织按角色族和工作项复杂度产生可解释排行与报告草稿；撤销插件立即停止新调用并终止逾期实例。

验收必须证明超额请求在提交前被拒绝，撤销插件阻止新的敏感调用并强制终止逾期运行实例，仪表盘不读取私人聊天内容，跨角色不能互相排行，样本不足时不展示名次，成员可以查看和更正自己的分析明细，并验证 500 人企业群的群日志写入不产生按成员数量线性增长的同步队列写入；还必须验证唯一所有者注销被阻塞至完成转让、账号匿名化后协作事实与序列完整性不被破坏、导出内容按请求者权限逐条裁剪，以及管理员导出留下审计并通知被涉及成员。

### 44.5 P4：企业版、安全、分片与端到端加密

交付 L3 企业提供者：按组织分片、DDoS/WAF、KMS/HSM、加密备份、受控出口、隔离执行、逐条哈希链与外部不可篡改审计归档、目录同步、密钥轮换、风险地理位置管制、黑名单、恢复演练、数据导出/删除、多设备和历史同步；实现独立审计过的私聊/群端到端加密、`webauthn` 与企业单点登录强度传递，以及按 `OrganizationEncryptionPolicy` 降级的设备侧本地索引。

用户闭环为：企业目录把成员和部门同步到组织授权插件，安全插件对异常设备发出邮件和站内风险处置，用户完成重新认证后一键停止异地设备；管理员根据 E2EE 功能矩阵确认降级后启用加密，Bot、搜索和分析入口同步隐藏或换为客户端能力；故障恢复插件用已签名检查点和流高水位对账，确认无分叉后再恢复写入。

验收必须覆盖 relay 指纹变更、中间人模拟、设备密钥撤销、异地登录安全邮件与一键停止设备、异常地点受控与恢复、缓存权限失效、分片故障转移、Relay 从备份回滚后的水位对账、迁移双读/双写与历史库恢复、流量攻击、私有地址和 DNS rebinding 的 SSRF 拒绝、离线成员的 E2E 密钥轮换、群成员变化后的旧/新 epoch 隔离、审计哈希链断裂检测，以及 E2E 模式下 Bot/搜索/分析入口按功能矩阵正确隐藏或降级、服务端正文索引被强制禁用且设备侧索引区间对用户可见。

## 45. 测试与验收策略

每个里程碑的验收条件已在迭代计划中给出，本节定义支撑这些条件的测试分层与长期回归资产。

测试分为五层，各层职责不重叠：

| 层次 | 覆盖对象 | 要求 |
|---|---|---|
| 单元 | 状态机、权限纯函数、schema、错误码映射 | 每个状态转换与每条拒绝路径都有聚焦用例；封闭联合类型用 `assertNever` 保证穷尽 |
| 契约 | `@dsh-chat/contract` 的命令、事件与版本协商 | 跨包共享的编解码与兼容窗口行为，含旧版本 host 连新 relay |
| 集成 | 单 relay + 多 host 的真实流程 | P0 两实例、P1 起三实例；使用真实数据库而非内存替身 |
| 恢复 | 崩溃、重启、回滚、分叉 | ACK 前崩溃、relay 重启、备份回滚后的水位对账、`sync_diverged` 处置 |
| 安全 | 越权、注入、隔离、供应链 | 见下，且必须随版本长期保留为回归资产 |

**安全回归用例库是交付物而不是一次性验证**，至少覆盖：跨组织读写尝试、角色越权与 ACL 绕过、成员移除后的访问拒绝、缓存过期授权、历史授权范围外读取、消息撤回后的索引残留、搜索结果计数推断、附件授权绕过、跨源与 CSRF 缺失、凭证与签名重放、时间偏移滥用、私网与 DNS rebinding 的 SSRF、确认挑战重放与刷新绕过、命令注入与富文本二次解析、Bot 与 AI 输出中的指令注入、插件撤销后的迟到结果、沙箱逃逸尝试（文件系统、环境变量、网络、Docker socket）、秘密泄漏到日志与产物、E2EE 模式下服务端正文访问尝试。

**每个安全用例断言的是拒绝行为与错误码，而不仅是“未崩溃”**；新增功能若触及授权、内容授权、出站或执行路径，**必须**同时补充对应的拒绝用例才能合入。

性能与容量测试对照 SLO 表执行，至少覆盖：大群日志写入不随成员数线性扇出、队列深度与游标滞后在目标内、限流在各维度按最严格者生效、大文件分块上传与断点续传、长连接背压下不丢已接收消息与授权变更。

迁移测试使用真实历史数据库快照，验证五步迁移的每一步**可暂停、可重试、可回滚读路径**，并在双读/双写期间校验计数、摘要与外键一致。

用户可见的 DSH 集成通过可运行 profile 提供无密钥 replay/snapshot 覆盖，使界面与事件流变更可在无外部依赖时回归。**测试数据不得包含真实凭证、真实组织数据或可用密钥。**

---

# 九、契约与规范附录

本篇是实现期的速查参考。错误码、术语与编码规范都属于 `@dsh-chat/contract`，各包必须使用同一口径。

## 46. 错误码目录

错误码属于 `@dsh-chat/contract`，是可判别联合的一部分。用户可见错误码与运维诊断信息分开记录；**用户错误码不得泄露其他组织、成员、文件或群的存在性**。

可重试性是错误码的固有属性，不由调用方猜测：`retryable` 表示同一幂等键重试可能成功，`terminal` 表示重试无意义，`conditional` 表示需先解决前置条件（如重新确认、补拉、扩容）后再试。

| 错误码 | HTTP | 类别 | 可重试性 | 幂等语义 |
|---|---:|---|---|---|
| `UNAUTHENTICATED` | 401 | 认证 | conditional | 重新认证后可重试 |
| `TIME_SKEW` | 401 | 认证 | conditional | 按签名服务器时间修正偏移后重试 |
| `SERVER_IDENTITY_MISMATCH` | 495 | 认证 | terminal | 需管理员确认签名轮换，禁止自动重试 |
| `DEVICE_RESTRICTED` | 403 | 风险 | conditional | 完成风险处置后可重试 |
| `DEVICE_REVOKED` | 403 | 风险 | terminal | 需重新注册设备 |
| `FORBIDDEN` | 403 | 授权 | terminal | 权限变更前重试无意义 |
| `NOT_FOUND_OR_FORBIDDEN` | 404 | 授权 | terminal | 统一返回，不区分存在性 |
| `RECIPIENT_INACTIVE` | 409 | 授权 | terminal | 目标已离开组织 |
| `CONFIRMATION_REQUIRED` | 428 | 确认 | conditional | 需先取得确认挑战 |
| `CONFIRMATION_EXPIRED` | 428 | 确认 | conditional | 必须重新展示摘要并确认 |
| `VERSION_CONFLICT` | 409 | 并发 | conditional | 读取最新版本后以新版本号重试 |
| `SYNC_DIVERGED` | 409 | 一致性 | conditional | 进入对账流程，停止自动重发 |
| `MEMBER_LIMIT_REACHED` | 402 | 配额 | conditional | 需扩容或释放名额 |
| `STORAGE_QUOTA_EXCEEDED` | 402 | 配额 | conditional | 需清理或扩容 |
| `BOT_BUDGET_EXCEEDED` | 402 | 配额 | conditional | 需管理员调整预算 |
| `RECIPIENT_QUEUE_FULL` | 507 | 容量 | conditional | 接收方消费后可重试，发送未被接收 |
| `GROUP_LOG_CAPACITY_EXCEEDED` | 507 | 容量 | conditional | 需保留策略或容量处理 |
| `SANDBOX_QUOTA_EXCEEDED` | 200 | 执行 | terminal | 尝试状态为 `failed`，保留检查点 |
| `RESOURCE_SNAPSHOT_FORBIDDEN` | 403 | 内容策略 | terminal | 目录禁止版本快照 |
| `SHARE_UNAVAILABLE` | 410 | 内容状态 | terminal | 已过期或已撤销 |
| `RESOURCE_GONE` | 410 | 内容状态 | terminal | 已物理清理 |
| `ATTACHMENT_UNAVAILABLE` | 200 | 内容状态 | conditional | 消息仍持久化，附件保留期内重试 |
| `EGRESS_BLOCKED` | 403 | 出站 | terminal | 目标不在 allowlist 或命中私网防护 |
| `PLUGIN_REVOKED` | 403 | 供应链 | terminal | 版本已撤销，能力租约失效 |
| `NOT_IMPLEMENTED` | 501 | 部署 | terminal | 该部署层未装载对应插件 |
| `RATE_LIMITED` | 429 | 限流 | retryable | 含 `retryAfter`，不泄露他人用量 |
| `REVIEW_REQUIRED` | 409 | 评审 | conditional | 项目策略要求评审结论后才能完成 |
| `REVIEW_SUPERSEDED` | 409 | 评审 | conditional | 关联产物已变化，需重新评审 |
| `DEPENDENCY_CYCLE` | 409 | 工作项 | terminal | 依赖关系将形成环 |
| `ARTIFACT_NOT_ACCEPTED` | 409 | 协作 | conditional | 候选产物需有写权限成员确认 |
| `PROTOCOL_VERSION_UNSUPPORTED` | 426 | 协议 | terminal | 需升级 host 或 relay |
| `SERVICE_READ_ONLY` | 503 | 恢复 | retryable | 处于 `read_only_recovery` |

`SANDBOX_QUOTA_EXCEEDED` 与 `ATTACHMENT_UNAVAILABLE` 映射为 200，因为它们是被正常返回的领域状态而不是请求失败；调用方按状态机处理，不按 HTTP 错误处理。

**权限拒绝、schema 无效、配额不足、资源不存在和版本冲突是终态错误，异步任务不得对其自动重试。**

## 47. 术语表

以下术语在 `@dsh-chat/contract` 中有唯一定义，各包实现必须使用同一口径，不得在插件内重新定义同名概念。

| 术语 | 含义 |
|---|---|
| `OrganizationId` / `WorkspaceId` / `ProjectId` | 服务端生成的品牌化不透明 ID，构成三级归属层次 |
| `AccountId` / `DeviceId` | 账号与设备标识；账号可有多设备，设备私钥不上传 |
| `MessageId` | 客户端生成的 UUIDv7，与发送者组成幂等键 |
| `MessageRevision` | 单调递增的消息版本号，编辑追加事件而非覆盖 |
| `DeliverySeq` | relay 为私聊收件人队列项分配的单调序列，按接收人分区 |
| `GroupLog` | 按 `GroupId + membershipRevision` 分区的群消息追加日志 |
| `GroupMessageSeq` | 群日志内单调递增序列，成员各自持游标读取 |
| `ThreadMessageSeq` | 线程内单调序列，仍存储于所属群日志分区 |
| `membershipRevision` | 群成员关系版本，仅表示成员变更，不表示已读 |
| `AccountStateSeq` | 账户级状态变更流序列，用于跨设备已读与偏好同步 |
| `StreamEpoch` / `HighWatermark` | 不可回退的流代次与已公布水位，用于故障恢复对账 |
| `BlobObject` | 内容寻址的物理字节对象，被消息与资源共同引用 |
| `ResourceVersion` | 组织共享存储中的不可变资源版本 |
| `MessageContentGrant` | 绑定消息、接收人范围与保留期的内容读取授权 |
| `GroupHistoryGrant` | 群历史读取授权，含范围上限游标与有效期 |
| `ShareableItem` | 可共享对象的统一抽象，含访问级别与有效期 |
| `CollaborationSession` | 从个人会话显式选择上下文创建的协作单元 |
| `ExecutionProvider` | 在隔离沙箱中运行协作执行尝试的服务 |
| `RepositoryBinding` | 项目与群对仓库的绑定，含访问模式 |
| `CodeChangeRecord` | 经远端验证的提交与项目/工作项/AI 使用的关联记录 |
| `BotServiceAccount` | Bot 的独立身份，权限静态绑定，不随调用者提升 |
| `BotAccessGrant` / `DelegationGrant` | Bot 读取授权与最小范围不可转授委托 |
| `PlanLimits` | 版本化的套餐容量与配额配置 |
| `RetentionPolicy` | 版本化的各类对象保留期、配额与清理规则 |
| `ConfirmationChallenge` | 绑定操作摘要的一次性高风险动作确认 |
| `SecurityRiskEvent` | 设备风险事件，驱动 `restricted` 等管制状态 |
| `RecoveryKit` | 账号恢复材料，含恢复公钥与守护人阈值策略 |
| `AccountTombstone` | 账号注销后用于保留协作事实的不可逆匿名标识 |
| `ReviewId` | 工作项评审关口，`approved` 只对锁定的产物版本生效 |
| `CommentRevision` | 评论修订号，编辑追加而非覆盖 |
| `AuditEvent` | 记录事实与引用、不含内容的审计条目 |
| `ProtocolVersion` | host 与 relay 协商的协议版本 |

## 48. 编码规范

- 使用严格 TypeScript、品牌化不透明标识符、可判别联合类型，并对封闭的消息和命令类型使用 `assertNever`。
- 协议 schema 只放在 `@dsh-chat/contract`；只在 HTTP、数据库、文件系统、队列和浏览器请求这些边界解析。
- 错误码目录、`AuditEvent` 结构、`ProtocolVersion` 与术语表都属于 `@dsh-chat/contract`，插件不得自定义同名概念或私有错误码；新增错误码必须同时声明 HTTP 映射、可重试性与幂等语义。
- host 与 relay 在建立设备会话时协商协议版本，不兼容返回 `PROTOCOL_VERSION_UNSUPPORTED` 并停止组织写入；升级顺序固定为 relay 先升、host 后升，relay 必须能同时服务兼容窗口内的新旧协议。
- 界面文案走 DSH 既有 locale 机制，不在运行时拼接可翻译句子；时间以 UTC 存储与比较，保留期与有效期按服务端时间计算，长度上限按字素簇而非字节计算。
- 所有 Cordis 注册通过 `ctx.effect()` 或 `ctx.on()` 完成，并返回每个 disposer。
- 除 `@dsh-chat/contract` 外，身份、组织、消息、内容、通知、协作、仓库、Bot、分析、审计、host 和 client 均必须是可独立装载的 Cordis 插件；bundle 只负责配置和排序，不实现业务单例。
- 每项跨包能力都要有服务定义、服务提供者和消费者；服务定义不得导入具体 provider，消费者不得导入 relay、数据库或对象存储实现细节。
- 插件声明所需服务、配置 schema、权限、持久事件、启动/停止行为和健康状态；**缺少必需服务时加载失败，卸载时取消任务、关闭连接并释放租约，不能留下路由或事件监听**。
- 每个会改变状态的 relay 或 host 操作都使用参数化查询和明确事务边界。
- 套餐限制、预算阈值、角色比较组、排名权重、文件限制和保留期都是经 schema 校验的组织配置；**实现不得把部署可变值写成代码常量**。
- 心跳阈值、在线可见策略、仓库提供方、webhook 验签算法、允许访问模式和提交归因规则也属于版本化组织策略。
- 缓存键、数据库查询、对象存储路径、队列分区和异步任务都**必须**携带 `OrganizationId` 与权限/资源修订；**失效无法确认时默认拒绝访问**。
- 命令解析器只接受带用户会话、CSRF、设备签名和确认挑战上下文的结构化请求；**禁止从聊天正文、Bot 输出或任何富文本二次解析命令**。
- 插件权限、订阅限制和分析策略的判定由服务端纯函数统一执行，并以策略版本写入审计和分析结果，**确保后续可复算**。
- 所有出站网络重试必须有上限、随机退避、可取消且可观测。**超时必须留下可重试或终态失败，不能留下静默的模糊状态。**
- 浏览器组件只负责呈现。共享视图状态放入已声明的 DSH store；持久聊天状态放入 host 数据库。
- 使用安全 DOM 文本 API 渲染消息内容，并在展示前校验附件元数据。
- 每个状态转换都增加聚焦单元测试；每个发布里程碑增加真实两/三实例集成测试。用户可见的 DSH 集成还需要通过可运行 profile 提供无密钥 replay/snapshot 覆盖。

## 49. 国际化、时区与无障碍

界面文案通过 DSH 的既有 locale 机制提供，插件不自建翻译体系，也不在运行时拼接可翻译句子。首批交付简体中文与英文，缺失文案回退到默认语言并标记，不显示原始 key。

时间在服务端**一律以 UTC 存储与比较**，客户端按用户时区呈现。保留期、有效期、签收期限、聚合窗口和配额窗口的计算使用服务端时间，不受客户端时钟影响；设备时钟偏移只影响请求签名，按 `TIME_SKEW` 处理。**跨时区团队的截止时间必须显示时区标识。**

文本处理按 Unicode 规范：显示名与群名做规范化与同形字检测，防止冒充；文件名净化保留非 ASCII 字符而不是转写丢失；长度上限按字素簇而非字节计算。

无障碍要求所有交互可键盘完成、焦点顺序与视觉顺序一致、状态变化对屏幕阅读器可感知、**颜色不作为唯一状态信号**（投递状态、风险状态、在线状态都必须有文本或图标），并遵循 DSH 主题 token 的对比度约定。**未读计数、投递失败和风险处置这类关键状态必须有非视觉表达。**

## 50. 开放决策

以下问题尚未定论，需要在对应阶段开始前确定。括号内为最晚需要答案的阶段。

### 50.1 产品与范围

- P1 前是否需要支持群主转让和群主丢失恢复？（P1）
- 线程、表情回应和转发的定级是否需要调整，特别是线程在 P1 缺席期间群内长讨论的替代方案？（P1）
- 语音消息是否作为文件子类型启用，若启用则录制时长、转码、转写和保留期如何限制？（P2）
- 组织是否需要部门、成本中心和跨工作区经理等更复杂的层级，还是先保持组织/工作区/项目三级？（P3）
- 评审是否允许项目级配置为可选，若可选则如何在跨项目排行中消除“无需评审”带来的口径差异？（P3）
- 工作项跨项目依赖是否在 P1 开放，还是先限制为同项目内依赖？（P1）

### 50.2 安全与合规

- 第二验证因素是否在 P0 即对组织所有者强制，`webauthn` 能否提前到 P3？（P0）
- 组织所有权接管的公示期长度、守护人阈值和部署管理员介入边界如何按组织类型区分？（P1）
- P1 允许哪些文件类型？对公网 relay 的恶意软件扫描和内容安全承担什么责任？（P1）
- 审计哈希链在 L2 是每日锚点还是逐条链接，外部归档由谁托管与验证？（P2）
- 账号注销宽限期与组织删除宽限期是否允许按组织或合同调整，法律保留如何覆盖匿名化流程？（P3）
- P4 是否允许端到端恢复密钥托管，还是仅允许用户离线恢复包和守护人阈值恢复？（P4）
- 端到端加密模式下设备侧本地索引的存储上限、跨设备差异提示和“不做本地索引”选项如何取舍？（P4）
- 端到端加密群的成员规模上限是多少，500 人群的成员密钥管理协议开销、epoch 轮换频率与离线成员补齐策略如何权衡？（P4）
- 数据驻留是否按区域强制，多区域部署下组织分片、备份、审计归档和出口代理各自的落地区域如何约定？（P4）

### 50.3 集成与运维

- 公共群 Bot 由 relay 运维者、群主还是独立可信服务账号托管？（P2）
- 首批支持哪些代码仓库提供方，如何处理自建 Git 服务、镜像仓库、作者邮箱映射和强制推送？（P1）
- 企业目录同步、单点登录和私有化部署采用哪些身份提供者与审计导出格式？（P4）
- 在线/空闲/离线阈值是多少，组织是否允许按项目订阅成员状态变化？（P0）
- 哪些通知可由组织标记为紧急，以及这种通知如何避免被滥用？（P1）
- 限流基线表中的默认值是否需要按组织类型分档，自建组织能否自行放宽消息与搜索速率？（P1）
- 各部署层的 SLA、RTO 与 RPO 数量级如何写入合同，RPO 与已公布高水位的间隔如何对齐？（P3）
- 数据导入与跨平台迁移支持哪些来源，作者映射与历史授权如何重建，导入内容如何在审计与排行中标记？（P3）
- L1 SQLite 到 L2 PostgreSQL 是否支持原地升级，还是只支持导出/导入？该问题影响自建组织的升级承诺，需在 P1 前明确。（P1）

### 50.4 商业化

- 团队版与企业版各自的默认人数、存储、Bot、模型预算和支持范围如何定价，哪些能力允许自建组织使用？（P3）
- 企业版大群的最大规模、群日志分区阈值和历史授权上限如何按合同与区域部署确定？（P4）
