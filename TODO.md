# 实现进度

> 本文件跟踪 `P0-a` 的实现进度。**范围定义来自[迭代计划 §44.1.1](./docs/04-roadmap/03-iteration-plan.md#4411-两个验收关口)，不在这里另立标准。**
>
> 阶段划分是实现顺序的工程安排，不是文档概念；每个阶段对应的验收依据都标注了文档出处。

最后更新：2026-08-30 · 24 个 PR 已合并 · 335 个测试

---

## 总览

| 阶段 | 内容 | 状态 |
|---|---|:---:|
| 0 | 仓库治理与开源规范 | ✅ |
| 1 | 工程骨架与 vendored 运行时 | ✅ |
| 2 | 插件装载形态验证 | ✅ |
| 3 | `@dsh-chat/contract` 契约层 | ✅ |
| 4 | `@dsh-chat/kernel` 入口包，装入 DSH | ✅ |
| 5 | 持久化层（SQLite schema 与迁移） | ✅ |
| 6 | 身份、组织与权限 | ✅ |
| 7 | 联系人与文本私聊 | ✅ |
| 8 | 工作项与通知 | 🔶 |
| 9 | 审计 | ✅ |
| 10 | 客户端界面 | 🔶 |
| 11 | 集成验收 | 🔶 |

✅ 完成　🔶 主体完成，有剩余项　⬜ 未开始

---

## 当前可用的能力

经**真实 HTTP** 走通的命令：

| 端点 | 能力 |
|---|---|
| `POST /api/chat/messages` | 发送私聊：准入判定、幂等、队列容量、审计同事务 |
| `POST /api/chat/messages/pull` | 按设备租约拉取批次 |
| `POST /api/chat/messages/ack` | 确认投递，返回实际确认数与请求数 |
| `POST /api/chat/messages/edit` | 编辑，追加事件，只接受更高 revision |
| `POST /api/chat/messages/revoke` | 撤回，tombstone 事件，幂等 |
| `POST /api/chat/work-items` | 创建工作项，需 `project.create` |
| `POST /api/chat/work-items/assign` | 分派并在同事务发通知 |
| `POST /api/chat/work-items/dependencies` | 添加依赖，成环返回 `DEPENDENCY_CYCLE` |
| `POST /api/chat/notifications` | 读收件箱，按游标补拉 |
| `POST /api/organization` | 创建组织，创建者成为 active 的所有者 |
| `POST /api/organization/workspaces` | 创建工作区，需 `workspace.create` |
| `POST /api/organization/projects` | 创建项目，按 `organization_id` 过滤工作区 |
| `POST /api/organization/members/invite` | 邀请成员，进入 `invited` 而非 `active` |
| `POST /api/organization/members/accept` | 接受邀请，仅本人，带版本检查 |
| `POST /api/organization/members/me` | 只返回调用者自己的成员关系 |
| `GET /api/chat/health` | 健康检查 |

所有写端点都有跨源防护、认证注入与审计同事务。

**尚不可用**：第二因素与恢复、在线状态、本地搜索、SSE 推送、群聊、附件。这些入口按文档要求显式缺失，**不伪装为可用**。

---

## 各阶段明细

### 阶段 0–2、4、5、9 · 已完成

- **治理**：MIT、双语文档、Issue/PR 模板、CI、分支保护（`main` 强制 PR + 2 项检查 + 线性历史，管理员同样受限）
- **工程**：Yarn 4 workspace、严格 TypeScript、vendored 运行时 `0.1.2-alpha.1`（5 个包，带外锚点校验）
- **插件形态**：装载/卸载测试，卸载后路由不残留，连续装卸 3 轮不冲突
- **入口包**：`kernel` bundle 已进入 DSH 的 149 条目插件树
- **持久化**：SQLite schema 含 §27 要求的全部七类必备字段，迁移单调且单事务原子
- **审计**：仅追加、与领域写入同事务、被拒绝的尝试同样留痕、表中无消息正文

### 阶段 3 · 契约层 ✅

| 项 | 状态 |
|---|:---:|
| §46 错误码目录 32 条，与文档双向锁定 | ✅ |
| 11 组领域状态集合，与文档双向锁定 | ✅ |
| 品牌化标识符与持久化类型 | ✅ |
| 命令与事件的 schema 定义 | ✅ |
| `AuditEvent` 的契约层定义 | ✅ |
| 协议版本协商的编解码 | ✅ |

> 状态**转换边**刻意未实现 —— 文档只约束了 `in_review → done` 等三条，自行发明转换表等于在实现中补需求。
>
> 16 个领域事件名取自 §6.1 能力矩阵，与错误码目录同一套反向解析防漂移机制。
>
> `ProtocolVersion` 此前实现为 `'1.0'` 字符串，与 §41 的「单调递增整数」不符 —— 字符串序下 `'1.10' < '1.9'`，已改为整数。

### 阶段 6 · 身份、组织与权限 ✅

| 项 | 状态 |
|---|:---:|
| 数据库以 Cordis 服务提供给领域插件 | ✅ |
| 邀请码签发与一次性消费 | ✅ |
| 双层授权判定，10 个角色的默认能力表 | ✅ |
| 组织/工作区/项目，版本控制与冲突检测 | ✅ |
| 成员邀请与接受 | ✅ |
| 设备注册与请求签名证明 | ✅ |
| 组织切换时的缓存隔离 | ✅ |
| 组织与成员的 HTTP 端点 | ✅ |

> 请求签名的**检查顺序**由测试锁定，不只是「都检查了」：§7.1 规定 `TIME_SKEW` 只在签名等其他条件正确时返回，先查时间的话任何人拿垃圾签名就能换到一份服务器签名的时间。nonce 同理放在最后 —— 先记再验会让攻击者填满受害设备的 nonce 空间。
>
> 跨组织隔离靠 `organization_id` 显式过滤，不靠授权判定：调用者在自己组织有 owner 成员关系时，作用域链走上去是会通过的。三处安全边界都做过变异验证。

### 阶段 7 · 联系人与文本私聊 ✅

| 项 | 状态 |
|---|:---:|
| 联系人请求、接受、拒绝、删除 | ✅ |
| 有向拉黑，双向独立判定 | ✅ |
| 私聊准入 `contactAccepted && !Block(a,b) && !Block(b,a)` | ✅ |
| 消息与队列项同事务，`DeliverySeq` 单调分配 | ✅ |
| `(senderId, messageId)` 幂等 | ✅ |
| 队列满在写入前拒绝，不淘汰未 ACK 消息 | ✅ |
| 租约按设备，ACK 幂等，到期重投 | ✅ |
| 发送、拉取、ACK 的 HTTP 端点 | ✅ |
| 发送方本地 `pending → accepted` 状态机 | ✅ |
| 消息编辑与撤回 | ✅ |

> 发送方是**三态**而非两态。只有 `pending`/`accepted` 的话终态失败无处安放，只能表现为「一直发送中」—— 用户看着转圈，永远不知道这条其实已经不会发出去了。没有 `delivered` 态：发送方无从得知接收方是否真收到。
>
> 编辑追加事件而非覆盖，`messages.body` 始终是初始正文。撤回也占一个 revision，使「撤回」与「迟到的编辑」能按同一把尺子比较。

### 阶段 8 · 工作项与通知 🔶

| 项 | 状态 |
|---|:---:|
| 工作项创建、分派、版本控制 | ✅ |
| 签收状态机（独立于工作项状态） | ✅ |
| 评审关口 `REVIEW_REQUIRED` | ✅ |
| 依赖成环检测，返回环路径 | ✅ |
| 通知去重、游标补拉、`seen`/`read` 区分 | ✅ |
| 创建、分派、依赖、收件箱的 HTTP 端点 | ✅ |
| 通知聚合（5 分钟窗口） | ⬜ |
| outbox 消费与 SSE 推送 | ⬜ |
| 评论与 `CommentRevision` | ⬜ |

### 阶段 10 · 客户端界面 🔶

| 项 | 状态 |
|---|:---:|
| 离线三态呈现（无一态可声称已送达） | ✅ |
| 错误按可重试性分级 | ✅ |
| 不做推断性描述 | ✅ |
| 事件流状态必须显式呈现 | ✅ |
| 状态面板组件与 CSS Modules | ✅ |
| slot 注册 | ✅ |
| 会话列表与消息视图 | ⬜ |

> 早先认为 slot 注册被类型依赖挡住 —— `settings.section` 由 `@deepseek-ai/dsh-client-ui-settings` 声明，其**传递**闭包是 54 个包 / 1.9 MB。
>
> 那个判断是错的。那些传递依赖只在 ui-settings 自己的 `.d.ts` 中被引用，而 `skipLibCheck: true` 会跳过对 `.d.ts` 的类型检查、**同时仍然处理其中的模块增强**。因此只 vendor 这一个包（28.6 KB）即可拿到 slot 键类型。

### 阶段 11 · 集成验收 🔶

| 项 | 状态 |
|---|:---:|
| 骨架第 1、3、5、6、7、8、9、11、14 步端到端跑通 | ✅ |
| relay 重启（真实磁盘文件，非内存库） | ✅ |
| 走查记录由测试自动生成 | ✅ |
| 在真实 DSH Desktop v2.0.4 上的装载验证 | ✅ |
| 经 HTTP 的私聊与工作项收发 | ✅ |
| 跨源写请求防护 | ✅ |
| §44.1.2 的其余失败路径（凭证重放、时间偏移滥用等） | ⬜ |
| 真正的两个 host 进程 + 一个 relay 进程 | ⬜ |

> 证据：[骨架走查记录](./docs/_meta/skeleton-walkthrough.md)（测试生成）、[DSH 装载验证](./docs/_meta/dsh-integration-evidence.md)。
>
> 当前的「两实例」是**同一进程内的两个用户身份**，不是两个独立的 host 进程。后者需要 relay 客户端，列在本阶段自己的剩余项里。

---

## 阻塞项

| 缺口 | 影响 | 处置 |
|---|---|---|
| 工作项完整转换矩阵未定义 | 只实现文档明确约束的关口 | 需文档变更 |
| 签收状态与工作项状态的耦合未说明 | 两台状态机各自独立演进 | 需文档变更 |
| 联系人**关系**状态枚举不完整 | `removed` 单列在实现侧 | 需文档变更 |

> 完整缺口登记见[实现记录 §3](./docs/_meta/implementation-log.md)。

## 不在 `P0-a` 范围内

`P0-b`：第二验证因素与备用码、设备丢失恢复、在线可见范围、host 本地搜索、协议版本协商的完整验收（骨架第 2、4、10、12、13 步）。

`P1` 及之后：群聊、附件与资源库、协作会话、群 Bot、公共插件目录、分析与计费。

**这些能力的入口当前显式缺失或返回 `NOT_IMPLEMENTED`，不伪装为可用。**
