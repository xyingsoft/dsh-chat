# 实现进度

> 本文件跟踪 `P0-a` 的实现进度。**范围定义来自[迭代计划 §44.1.1](./docs/04-roadmap/03-iteration-plan.md#4411-两个验收关口)，不在这里另立标准。**
>
> 阶段划分是实现顺序的工程安排，不是文档概念；每个阶段对应的验收依据都标注了文档出处。

最后更新：2026-08-30

---

## 总览

| 阶段 | 内容 | 状态 |
|---|---|:---:|
| 0 | 仓库治理与开源规范 | ✅ |
| 1 | 工程骨架与 vendored 运行时 | ✅ |
| 2 | 插件装载形态验证 | ✅ |
| 3 | `@dsh-chat/contract` 契约层 | 🔶 |
| 4 | `@dsh-chat/kernel` 入口包，装入 DSH | ✅ |
| 5 | 持久化层（SQLite schema 与迁移） | 🔶 |
| 6 | 身份、组织与权限 | 🔶 |
| 7 | 联系人与文本私聊 | 🔶 |
| 8 | 工作项与通知 | ⬜ |
| 9 | 审计 | ⬜ |
| 10 | 客户端界面 | ⬜ |
| 11 | 两实例集成验收 | ⬜ |

✅ 完成　🔶 进行中　⬜ 未开始

---

## 已完成

### 阶段 0 · 仓库治理

- MIT 许可证，与 DSH 生态一致
- 中英双语 `README` / `CONTRIBUTING` / `CODE_OF_CONDUCT`
- `SECURITY.md`，漏洞报告走 GitHub 私密通道（已启用）
- Issue 模板三种（缺陷 / 能力需求 / 文档变更）与 PR 模板，内置文档先行检查清单
- CI：文档链接一致性 + 构建测试
- 分支保护：`main` 强制 PR、强制 2 项检查、线性历史、管理员同样受限
- `THIRD_PARTY_NOTICES.md` 声明 vendored 组件来源与许可

### 阶段 1 · 工程骨架

- Yarn 4 workspace，`packageManager` 固定版本
- 严格 TypeScript（`noUncheckedIndexedAccess`、`exactOptionalPropertyTypes`、`verbatimModuleSyntax`）
- vendored DSH 运行时 `0.1.2-alpha.1`，对应 DSH Desktop `v2.0.4`
- `scripts/verify-vendored-runtime.mjs`：校验和、体积、包名版本、`resolutions` 覆盖、声明范围必须精确，并有与 manifest 分离的带外锚点
- `scripts/check-links.sh`：跳过围栏代码块、剥离链接标题、支持根绝对路径、零检查时报错

### 阶段 2 · 插件装载形态

- host 插件在真实 Cordis 容器 + 真实 `WebServer` 下的装载测试
- **卸载后路由不残留**（§48 硬约束），并以「连续装卸 3 轮不抛错」交叉验证

### 阶段 4 · 入口包

- `@dsh-chat/kernel` 携带 `dsh.bundle.patch` 与 `cordis.patch.yml`
- 装载契约测试：逐条 `import` 补丁中的包名，断言导出 `apply` 且无 `default` export
- 已装入本机 DSH profile，`dsh --dump-config` 中可见 `chat-host` 条目

---

## 进行中

### 阶段 3 · 契约层 🔶

| 项 | 状态 |
|---|:---:|
| §46 错误码目录 32 条（HTTP 映射、可重试性、幂等语义） | ✅ |
| 11 组领域状态集合 | ✅ |
| 与文档的双向防漂移测试 | ✅ |
| 品牌化标识符与持久化类型（`EventId`、`OperationId`、`EncryptionMeta` 等） | ✅ |
| 命令与事件的 schema 定义 | ⬜ |
| `AuditEvent` 结构 | ⬜ |
| 协议版本协商的编解码 | ⬜ |

> 状态**转换边**刻意未实现。文档只给出状态集合，除 `in_review → done` 需评审关口等少数几条外没有定义合法转换；在实现里补一张转换表等于绕过文档先行流程。

### 阶段 5 · 持久化层 🔶

| 项 | 状态 |
|---|:---:|
| SQLite schema：账号、设备、恢复材料、邀请码 | ✅ |
| 组织三级层次与成员关系 | ✅ |
| 联系人请求与有向拉黑 | ✅ |
| 消息、投递队列、流代次与高水位 | ✅ |
| 工作项与依赖、通知、审计、事务 outbox | ✅ |
| 迁移执行器（版本单调、单事务原子） | ✅ |
| §27 七类必备字段的存在性测试 | ✅ |
| 仓储层（各领域的读写函数） | ⬜ |
| 五步迁移的演练用例 | ⬜ |

> 已按 §29.1 落地 `encryption_meta`、事件格式版本、账户/设备同步状态 —— P0 全部是默认值，但先占位比 P4 重写消息主表便宜得多。

---

## 未开始

### 阶段 6 · 身份、组织与权限 🔶

对应骨架第 1、3 步。

| 项 | 状态 |
|---|:---:|
| 数据库以 Cordis 服务提供给领域插件 | ✅ |
| 邀请码签发与**一次性消费**（条件更新保证并发安全） | ✅ |
| 三种消费失败统一返回 `NOT_FOUND_OR_FORBIDDEN`，诊断只进服务端 | ✅ |
| 双层授权判定：角色默认能力 + 作用域链合并 | ✅ |
| 10 个角色的默认能力表，对齐 §11.1 | ✅ |
| 组织/工作区/项目的创建与版本控制 | ✅ |
| 成员邀请与接受 | ✅ |
| 设备注册与请求签名证明 | ⬜ |
| 组织切换时的缓存隔离 | ⬜ |
| host 路由（命令入口） | ⬜ |

> 角色的英文标识符是已登记的文档缺口 —— §11.1 的角色表全为中文。当前取值是实现侧命名，**不是**文档定义的品牌化 ID；文档给出权威命名后以文档为准。

### 阶段 7 · 联系人与文本私聊 🔶

对应骨架第 6、7、9、11 步。

| 项 | 状态 |
|---|:---:|
| 联系人请求、接受、拒绝、删除（`removed` 不等于拉黑） | ✅ |
| 有向拉黑 `Block(actor, subject)`，双向独立 | ✅ |
| 私聊准入 `contactAccepted && !Block(a,b) && !Block(b,a)` | ✅ |
| 消息与队列项同事务写入，`DeliverySeq` 单调分配 | ✅ |
| `(senderId, messageId)` 幂等，重试返回首次结果 | ✅ |
| 队列满在写入前拒绝，且不淘汰未 ACK 消息 | ✅ |
| 带租约的批次拉取，租约按**设备**而非账号 | ✅ |
| ACK 幂等，非租约持有者不能确认 | ✅ |
| 租约到期后重投（ACK 前崩溃的恢复） | ✅ |
| 发送方本地 `pending → accepted` 状态机 | ⬜ |
| 消息编辑与撤回（追加事件，不覆盖） | ⬜ |
| host 路由（发送、拉取、ACK 的命令入口） | ⬜ |

### 阶段 8 · 工作项与通知

对应骨架第 5 步。工作项创建与分派、九态状态机、依赖成环检测（`DEPENDENCY_CYCLE`）、独立的签收状态机、持久化收件箱与去重键、outbox 消费。

### 阶段 9 · 审计

对应骨架第 14 步。`AuditEvent` 仅追加存储、**与领域写入同事务**、被拒绝的越权尝试同样留痕、**审计写入失败导致整个命令失败**、审计表不含任何消息正文。

### 阶段 10 · 客户端界面

React slot 注册、会话列表与消息视图、工作项、通知收件箱、组织与账号设置。约束：客户端只负责呈现，不做权威缓存，不在浏览器中重算权限；离线三态不可混淆；错误按可重试性分级呈现。

### 阶段 11 · 两实例集成验收

两台 DSH Web 实例 + 一个持久化 relay，跑通[最小可运行骨架](./docs/04-roadmap/02-minimum-skeleton.md)的第 1、3、5、6、7、8、9、11、14 步，以及 §44.1.2 列出的失败路径。

---

## 阻塞项

| 缺口 | 影响阶段 | 处置 |
|---|---|---|
| 工作项状态转换边未定义 | 8 | 需文档变更；实现只落地文档明确约束的几条，其余保持宽松并注明 |
| 签收状态与工作项状态的耦合关系未说明 | 8 | 同上 |
| 非消息命令的幂等键字段名未给出 | 6–9 | 已在 contract 定义 `OperationId`，按 §48「协议 schema 只放在 contract」处理 |

> 完整缺口登记见[实现记录 §3](./docs/_meta/implementation-log.md)。

## 不在 `P0-a` 范围内

以下属 `P0-b` 或更后阶段，**当前必须显式返回 `NOT_IMPLEMENTED`，不得伪装为可用**：

第二验证因素与备用码、设备丢失恢复、在线可见范围、host 本地搜索、协议版本协商的完整验收（骨架第 2、4、10、12、13 步）；群聊、附件与资源库、协作会话、群 Bot、公共插件目录、分析与计费。
